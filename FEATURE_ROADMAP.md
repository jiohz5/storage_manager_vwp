# Storage Manager Feature Status

자동 삭제 없이 읽기 전용 모니터링과 안전한 야간 수집을 우선합니다.

## 다음 구현 확정: 적응형 주간 분석

### 목표

06:00~22:00를 상세 작업 완전 정지 구간으로 두지 않고, 짧고 재개 가능한 저부하
분석 구간으로 활용합니다. 주간 작업은 야간 전체 분석을 대체하지 않으며, 21:55에
항상 체크포인트를 저장하고 종료해 22:00 야간 작업에 실행권을 넘깁니다.

### 실행 모드

| 모드 | 대상 | 실행 제한 | 시작 방식 |
|---|---|---|---|
| 자동 주간 | 급증·고사용률·미완료 우선 계정 한 개 | 매시간 10분 | `10 6-21 * * *` cron |
| 수동 시간제 | GUI에서 선택한 계정 한 개 | 15분·30분·60분 | 사용자가 클릭 |
| 수동 연속 | GUI에서 선택한 계정 한 개 | 완료·안전 중지·21:55 중 먼저 도달 | 사용자가 클릭 후 확인 |
| 야간 전체 | 모든 활성 계정 | 22:00~06:00 | 기존 nightly cron |

- 모든 상세 작업은 공통 프로세스 잠금을 사용하고 동시에 한 계정만 순회합니다.
- 자동 주간은 최근 15분 표본에서 급증·95% 이상인 계정을 먼저 선택합니다.
- 경고 계정이 없으면 미완료 체크포인트가 가장 오래된 계정을 선택하고 날짜별 순환으로
  특정 계정이 계속 뒤로 밀리지 않게 합니다.
- 주간 모드는 `nice -n 19`, `ionice -c 3`을 사용합니다. 둘 중 하나라도 사용할 수
  없으면 자동 주간은 실행하지 않고 건강 점검 경고를 남깁니다.
- 수동 모드는 저우선순위 도구나 부하 기준을 만족하지 못하면 현재 수치를 보여주고
  확인받은 뒤 실행할 수 있지만, 21:55 종료 규칙은 우회할 수 없습니다.
- 야간 모드는 현재 `nice -n 10`, `ionice -c 2 -n 7`과 22:00~06:00 정책을
  유지합니다.

### 부하 안전장치

자동 주간 시작 전과 실행 중 30초마다 Python 표준 라이브러리와 `/proc`를 이용해
VWP 클라이언트 부하를 확인합니다.

- 1분 load average를 CPU 개수로 나눈 값이 70% 이상이면 자동 시작을 건너뜁니다.
- `/proc/pressure/io`의 `some avg10`이 제공되고 10% 이상이면 자동 시작을
  건너뜁니다.
- 실행 중 두 번 연속 기준을 넘으면 현재 하위 작업을 안전 중지하고 다음 시간으로
  미룹니다.
- PSI가 제공되지 않는 RHEL에서는 load average와 10분 duty cycle만 적용합니다.
- 이 값들은 NFS·Lustre·GPFS 서버 자체 부하가 아니라 VWP 클라이언트 관측값임을
  GUI와 로그에 명시합니다.

### 21:55 야간 인계

- 자동·수동 주간 작업은 21:55에 새 디렉터리 작업을 시작하지 않습니다.
- 실행 중인 `du`·`find`·검색 인덱싱 하위 작업에 안전 중지를 요청하고 완료된
  디렉터리 체크포인트를 보존합니다.
- 최대 2분 안에 하위 작업을 종료해 21:57까지 공통 잠금을 해제합니다.
- nightly cron은 22:00에 시작하고, 인계 지연이나 일시적인 cron 누락에 대비해
  22:10에 한 번 더 확인합니다. 이미 야간 작업이 실행 중이면 두 번째 호출은 성공
  상태의 no-op으로 종료합니다.
- 22:00 야간 작업은 주간 작업의 계정·디렉터리 체크포인트부터 이어서 전체 활성
  계정 큐를 처리합니다.

### 분석 진행률

기존의 `df 계정 수`와 `완료/전체 작업`만 표시하는 대신 일반 사용자에게 보이는
`분석` 탭을 추가합니다. `추적` 탭에는 cron·notifier·자동 실행 상태만 남기고 수동
분석 제어와 상세 진행 카드는 `분석` 탭으로 이동합니다.

진행 카드에는 다음 값을 항상 표시합니다.

- 실행 모드: 자동 주간, 수동 시간제, 수동 연속, 야간 전체
- 계정명과 현재 절대경로
- 분석 단계: `df`, 기준선 `du`, 변경 파일 `find`, 검색 인덱싱, 보고서
- 구간 실행시간, 구간 제한시간, 21:55 종료시각, 다음 자동 재개시각
- 완료 디렉터리 수, 현재까지 발견한 디렉터리 수, 확인한 파일 수
- 현재까지 분석한 할당 용량, 변경 파일 수, 변경 파일 현재 크기 합계
- 마지막 체크포인트 시각과 최근 오류

퍼센트는 다음 규칙으로 과장 없이 표시합니다.

- 첫 기준선에는 최종 계정 크기라는 분모가 없으므로 `현재 발견 작업 기준 약 N%`로
  표시합니다. 큰 디렉터리가 분할되면 분모가 늘어 퍼센트가 낮아질 수 있음을
  tooltip에 설명합니다.
- 완료된 기준선이 있으면 완료한 `du` 작업의 할당 용량을 이전 기준선 전체 용량으로
  나눈 `예상 용량 커버리지`를 함께 표시하고 완료 전에는 최대 99%로 제한합니다.
- 공유 파일시스템 전체 값인 `df` 사용량은 계정 분석 용량의 분모로 사용하지 않습니다.
- 변경 파일 `find`는 디렉터리 단위 체크포인트 큐로 변경해 `완료 디렉터리 /
  현재 발견 디렉터리`, 확인 파일 수, 변경 파일 수와 용량을 표시합니다.
- 검색 인덱싱은 완료·발견 디렉터리와 저장한 인덱스 항목 수를 표시합니다.
- 모든 동적 퍼센트에는 `약` 또는 `현재 발견 기준` 표기를 붙이고, 전체 순회가
  끝난 경우에만 정확한 100%를 표시합니다.

진행 카드 예시는 다음과 같습니다.

```text
자동 주간 저부하 | project_a | 기준선 용량 분석 (du)
현재 /user/project_a/results/run_42
현재 발견 작업 기준 약 63%
분석 8.4 TB / 이전 기준선 13.2 TB
디렉터리 1,842 / 2,917 | 실행 06:12 / 10:00 | 다음 재개 14:10
```

### 재개 가능한 변경 파일 분석

현재 일일 `find -newermt`는 완료해야 activity cursor가 전진하므로 짧은 주간 구간에서
반복 시작될 수 있습니다. 이를 디렉터리 작업 큐로 변경합니다.

- 스캔 시작시각, 디렉터리 큐, 완료 디렉터리, 집계 결과를 SQLite에 기록합니다.
- 디렉터리 하나가 제한시간을 넘으면 하위 디렉터리 작업으로 분할합니다.
- 중단 시 완료 디렉터리는 다시 읽지 않고 미완료 디렉터리부터 재개합니다.
- 전체 큐가 끝난 뒤에만 activity cursor와 일간 변경 집계를 확정합니다.
- 검색을 켠 계정은 같은 변경 파일 스트림을 재사용해 두 번째 순회를 만들지 않습니다.
- 계정 루트의 `.snapshot`, symlink, 다른 파일시스템 경계 제외 정책을 그대로
  적용합니다.

### 부하 로그와 피드백

30초 원본 표본은 데이터 디렉터리의 `scan_load/YYYY-MM-DD.csv`에 기록합니다.
CSV는 외부 패키지 없이 열 수 있고 실제 VWP 피드백에 바로 첨부할 수 있습니다.

기록 항목은 다음과 같습니다.

- 시각, run ID, 실행 모드, 계정, 분석 단계, 현재 경로
- 구간 경과시간과 제한시간
- 프로세스 CPU 시간·추정 사용률, RSS 메모리, `/proc/<pid>/io` read bytes
- 시스템 1분 load average, CPU 수, 정규화 load, CPU iowait
- I/O PSI `some/full avg10` 값 또는 `unavailable`
- 완료·발견 디렉터리, 확인 파일, 변경 파일, 분석 용량
- 중지 이유, 오류, 체크포인트 결과

원본 CSV는 30일 후 정리하고 실행별 평균·최대·95백분위 요약은 SQLite에 365일
보관합니다. GUI는 최근 실행의 평균/최대 CPU, load, iowait, PSI, 분석량, 실행시간을
보여줍니다. `/proc/<pid>/io`와 PSI가 공유 스토리지 서버의 실제 부하를 나타내지
않는다는 제한도 함께 표시합니다. 로그 기록 실패는 상세 분석을 중단시키지 않고
상태와 cron 로그에 경고로 남깁니다.

### Cron과 실패 감지

- 관리 cron에 `daytime` marker를 추가하고 `nightly`, `capacity`, `health`,
  `daytime` 네 항목이 모두 있어야 등록 완료로 표시합니다.
- 기존 사용자 cron 항목은 유지하며 `crontab -l` 실패 시 덮어쓰지 않습니다.
- 주간 실행 heartbeat와 마지막 종료 이유를 상태 파일에 기록합니다.
- 07시 건강 점검은 주간 cron 누락, 반복적인 부하 기준 초과, 21:55 인계 실패,
  22시 야간 미시작을 각각 구분해 알립니다.
- Python·데이터·스크립트 절대경로와 `PYTHONHOME` 사전 점검 정책을 유지합니다.

### 검증 기준

- 06:10~21:10 자동 실행은 정확히 10분 예산을 사용하고 체크포인트에서 재개합니다.
- 수동 15·30·60분과 수동 연속이 선택 계정 한 개에만 적용됩니다.
- 모든 주간 모드는 21:55에 중지를 시작하고 22:00 야간 작업이 이어받습니다.
- 높은 load·PSI에서 자동 실행은 시작하지 않으며 수동 실행은 경고를 표시합니다.
- 첫 기준선에는 추정 표기가 있고 이전 기준선이 있을 때만 용량 분모를 표시합니다.
- `du`, `find`, 검색 인덱싱 각각 현재 경로와 단계별 통계를 제공합니다.
- 30초 부하 CSV, 30일 정리, 365일 요약이 경계 날짜에서 동작합니다.
- cron 네 항목 등록·보존·부분 등록·실패와 22:10 no-op 재시도를 검증합니다.
- Windows 단위 테스트와 offscreen GUI 검증 후 실제 RHEL에서 `nice`, `ionice`,
  `/proc`, cron, 21:55 인계를 점검합니다.

## 이번 반영 완료

1. **inode 사용률**
   `df -Pi`를 byte 사용률과 함께 수집하고 90% 주의, 95% 경고를 적용합니다.
2. **Full 예상일**
   최근 7일·30일 기울기로 경고 기준과 100% 도달 예상일을 표시합니다. 표본 부족,
   감소·정체 추세, 10년 초과 예상은 `예측 불가`로 처리합니다.
3. **증가 이상 탐지**
   최근 양의 증가량 중앙값·MAD, 3배 배수, 최소 100GB 조건을 함께 사용합니다.
4. **수집 신선도 감시**
   계정별 마지막 nightly 수집 경과시간을 표시하고 07시 건강 점검 cron이 누락·실패를
   GUI 없이 알립니다.
5. **Quota·공유 filesystem·내부 알림**
   quota JSON command, filesystem별 계정 그룹, outbox/command/webhook 어댑터와
   cooldown을 제공합니다.
6. **정리 검토 후보**
   기본 100GB 이상, 30일 관찰, 최근 30일 활동 없음 조건의 보고서를 생성합니다.
   실제 삭제 명령은 실행하지 않습니다.
7. **15분 급증 감시와 MATE 로컬 알림**
   15분 `df` 표본으로 100GB 급증과 6시간·2시간 이내 FULL을 감지하고, GUI가
   닫혀 있어도 로그인 세션의 독립 트레이 notifier가 outbox 경고를 표시합니다.
8. **전역 데이터 위치와 런타임 진단**
   quota가 제한된 개인 계정에는 1KB 미만 위치 포인터만 두고 실제 데이터는 최초
   선택한 여유 있는 경로에 저장합니다. `run.csh --diagnose`가 Python·JSON·SQLite·
   PyQt5·그룹·쓰기 권한과 현재 관리 데이터 크기를 한 번에 확인합니다.
9. **관리자 전용 경로 검색**
   고정 PIN으로 현재 세션에서만 검색 탭을 노출하고, 계정별 opt-in 방식으로 파일·
   디렉터리 이름과 확장자를 별도 SQLite에 인덱싱합니다. 디렉터리 체크포인트로
   재개하며 일일 변경 스트림 재사용과 7일 전체 대조를 함께 사용합니다.
10. **간결한 운영 UI**
   계정명·경로 양방향 자동 완성, inode 도움말, 상태형 cron/scan/notifier 버튼,
   넓은 최근 수집 열, X 종료 시 cron·트레이 지속 안내를 제공합니다.

## 요청에 따라 제외

- 담당자, 과제명, due date, 작업/백업 계정 연결과 같은 계정 생명주기 메타데이터는
  이번 범위에서 제외했습니다.

## 실제 VWP 확인 후 고려

1. 파일시스템이 snapshot diff 또는 changelog API를 제공하면 전체 `find` 순회를
   대체할 수 있습니다. 30~50TB 환경에서 가장 큰 성능 개선 후보입니다.
2. 여러 관리자가 하나의 이력을 공유해야 하면 네트워크 SQLite 대신 중앙 수집기와
   읽기 전용 GUI/API 구조가 적합합니다.
3. 역할 기반 권한과 계정 편집·cron 변경·중지 요청 감사 로그를 추가할 수 있습니다.
4. 계획된 신규 데이터량을 가상으로 더하는 30/60/90일 용량 시나리오를 추가할 수
   있습니다.
