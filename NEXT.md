# 다음 작업 인계 메모

> 세션을 이어서 시작할 때 이 파일부터 읽으면 됩니다. 확정된 설계 배경은
> [DESIGN.md](DESIGN.md), 사용법은 [README.md](README.md)에 있습니다. 이 문서는
> **아직 안 끝난 것**만 담습니다. 작업이 끝나면 지워도 됩니다.

## 현재 상태

- 브랜치 `main`, 원격 `origin/main`과 동기화됨. 마지막 커밋 `9d590e8`.
- 테스트 **380 passed / 1 skipped**. 간헐 실패하던 테스트 두 개는 원인을 찾아
  고쳤으므로(아래 참고) 이제 스위트는 안정적이다.
- 개발 PC는 Windows + Anaconda Python 3.10, 반입 대상은 RHEL + Python 3.12다.
  GUI 확인은 위젯을 직접 grab해 PNG로 저장하는 방식을 썼다 (창을 밖에서
  캡처하면 DPI 배율과 다른 창 가림이 섞여 판단이 흐려진다).

## 지금 논의 중인 것: 사내 공용 배포 구조

### 확정된 환경 (사용자 확인)

- **서버 한 대에 10명 내외가 로그인**한다.
- 그중 **스캐닝 권한이 있는 담당자는 많아야 2~3명**.
- 계정은 6~10개 등록 예정.

### 사용자가 제안한 방향

계정마다 담당자를 한 명 지정하고, viewer 실행 파일과 관리 실행 파일을 나눠
권한을 구분한다.

### 검토 결과 — 그대로 가면 안 되는 부분이 있다

**"담당자별로 자기 계정만 스캔"은 이 환경에서 커버리지 구멍을 만든다.**

`scan_lock`은 데이터 디렉터리 안의 파일과 pid로 전체 실행을 하나만 허용한다
([scan_lock.py](smvwp/scan_lock.py)). 담당자 3명이 데이터 디렉터리를 공유하고
각자 22:00 cron을 걸면, **잠금을 먼저 잡은 한 명만 돌고 나머지 둘은 그냥
종료**한다. 그 두 사람이 담당하는 계정은 그날 밤 스캔되지 않는다.

서버가 한 대라는 것이 확인됐으므로 다음이 맞다:

- **야간 스캔은 사람 단위 작업이 아니라 서버 단위 작업이다.** cron은 서버에
  **하나만** 걸고, 그 한 번이 전체 계정을 기존처럼 직렬로 돈다.
- `Account.owner`(담당자)는 **스캔 분담용이 아니라** "이 계정은 누구에게
  물어보면 되는가"와 알림 대상 표시용이다. (지금 있는 `created_by`는 "등록한
  사람"이라 뜻이 다르다 - 담당자는 바뀔 수 있으므로 별도 필드가 맞다.)
- 담당자와 일반 사용자를 가르는 **실제 경계는 데이터 디렉터리 쓰기 권한**
  하나뿐이다. 실행 파일 분리는 실수 방지와 역할 안내이지 강제가 아니다
  (PIN에 대해 이미 문서에 적어 둔 것과 같은 성격이다). 이 사실을 README에
  같은 톤으로 명시해야 한다 - 안 적으면 "viewer니까 안전하다"고 오해한다.

### 실측으로 확인한 제약 (권한 설계에 직접 영향)

**SQLite를 `mode=ro`로 열어도 `-wal`/`-shm` 파일이 생성된다.** 실험 결과:

- 조회는 정상, 쓰기는 `attempt to write a readonly database`로 차단됨.
- 그런데 조회만 해도 디렉터리에 `t.db-shm`, `t.db-wal`이 새로 생겼다.

즉 **읽기 전용 사용자에게도 데이터 디렉터리 쓰기 권한이 필요하다.** 디렉터리를
`750`으로 잠그고 viewer에게 읽기만 주면 DB를 아예 열지 못한다. 선택지는:

1. 그룹 쓰기를 허용하고(`770`), 강제는 포기한 채 viewer 진입점으로 실수만 막는다.
2. 담당자가 보고서/스냅샷을 별도 경로에 떨궈 두고 viewer는 그것만 본다
   (DB를 아예 안 연다). 강제가 되지만 실시간성이 떨어진다.

NFS는 별개로 더 위험하다 - WAL은 NFS에서 동작하지 않으므로 데이터 디렉터리는
**반드시 서버 로컬 디스크**에 두어야 한다. 지금 README에는 이 경고가 없다.

### 제안한 작업 순서 (사용자 승인 대기 중)

1. `Account.owner` 추가 + 계정 표에 `담당자` 열
2. `smvwp_cli.py view` — 읽기 전용 진입점 (수집/스캔 스케줄러를 띄우지 않음).
   지금은 GUI를 열면 무조건 스케줄러가 뜬다 ([main_window.py](smvwp/gui/main_window.py))
3. cron은 서버에 하나만 — `setup_cron.csh`가 중복 등록을 감지하도록
4. 권한 모델 문서화 (위 "강제 vs 안내" 구분 + 로컬 디스크 필수 경고)

## 그 밖에 합의된 로드맵 (아직 미착수)

`du` 성능 분석에서 나온 네 가지 중 1번만 끝났다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | `du -k` 단일 순회로 전환 | **완료** (`9d590e8`) |
| 2 | 트리 뷰어 (ncdu/gdu 형태) | 미착수 — 1번이 데이터를 이미 만들어 둠 |
| 3 | 파일시스템 단위 병렬 | 미착수 — 계정들이 실제로 몇 개 볼륨에 걸쳐 있는지 먼저 확인 필요 |
| 4 | 증분 스캔 (mtime 기반) | 미착수 — 정확도 트레이드오프가 있어 가장 신중해야 함 |

**2번 트리 뷰어**가 다음 순서로 자연스럽다. `du -k`가 모든 하위 디렉터리 크기와
깊이를 `baseline_results`에 이미 저장하고 있다
(`scan_store.tree_entries()`). 평평한 목록은 깊이 0만 보여주도록 제한해 뒀으므로
(부모·자식이 나란히 떠서 합계가 중복되는 것을 막기 위함), 파고드는 경험은 트리
화면이 담당해야 한다.

**4번 증분 스캔 주의점**: 파일이 제자리에서 커지면 상위 디렉터리 mtime은 바뀌지
않는다(로그 append가 정확히 이 경우). 디렉터리 mtime만 믿으면 가장 중요한 증가를
놓친다. 반드시 **파일 단위** `find -newermt`로 판정해야 한다.

## VWP 실기에서 확인이 필요한 것들

개발 PC가 Windows라 여기서 검증할 수 없었던 것들:

- `du -k --max-depth` / `--exclude`가 실제 RHEL `du`에서 기대대로 도는지
- 창을 닫을 때 자식 프로세스 정리 (`/proc` 기반이라 리눅스에서만 실제 동작)
- 스캔 중 CPU/메모리 측정값이 실제로 어떻게 찍히는지
- 99% 즉시 알림 팝업 (실제로 그 상황이 되어야 확인 가능 -
  급하면 `config.json`의 `immediate_notify_pct`를 낮춰 시험)
- 일간 보고서가 매일 생성되는지 (`<data_dir>/reports/daily/`에 오늘 날짜 파일)

## 알아 둘 함정 (작업하다 걸렸던 것들)

- **Bash heredoc에서 `\n` 이스케이프가 깨진다.** 파이썬 코드를 heredoc으로
  넘길 때 `"\n".join(...)` 같은 문자열이 실제 줄바꿈으로 치환되어 구문 오류가
  났다. 백슬래시가 필요하면 `chr(92)`로 만들어 쓰는 편이 안전하다.
- **`git stash --keep-index`가 인덱스를 흐트러뜨렸다.** 커밋을 성격별로 나눌 때
  hunk 단위 스테이징과 겹치면서 파일이 꼬였다. 부분 스테이징이 필요하면
  "원하는 상태의 파일 내용을 직접 써서 `git add` → 원래 내용 복원"이 더 안전하다.
- **Qt 내장 정렬은 칸 위젯과 함께 쓰면 안 된다.** 항목만 옮기고 위젯은 제자리에
  남아 행과 어긋난다. 계정 표(막대·배지가 칸 위젯)는 직접 정렬해 다시 그린다.
- **QSS로 `::item`을 스타일링하면 `setBackground()`가 무시된다.** 행 배경색은
  델리게이트에서 직접 칠한다 (`widgets.TierRowDelegate`).
