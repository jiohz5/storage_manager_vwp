# 다음 작업 인계 메모

> 세션을 이어서 시작할 때 이 파일부터 읽으면 됩니다. 확정된 설계 배경은
> [DESIGN.md](DESIGN.md), 사용법은 [README.md](README.md)에 있습니다. 이 문서는
> **아직 안 끝난 것**만 담습니다. 작업이 끝나면 지워도 됩니다.

## 현재 상태

- 브랜치 `main`. 테스트 **451 passed / 1 skipped**.
- 개발 PC는 Windows + Anaconda Python 3.10, 반입 대상은 RHEL + Python 3.12다.
  GUI 확인은 위젯을 직접 grab해 PNG로 저장하는 방식을 썼다 (창을 밖에서
  캡처하면 DPI 배율과 다른 창 가림이 섞여 판단이 흐려진다).

## 이번에 들어간 것 (실기 검증 전)

> 아래 네 가지는 개발 PC(Windows)에서만 확인했다. **RHEL 실기에서 아직 안
> 돌려 봤다** - 특히 리소스 계측은 `/proc`이 없어 값이 전부 비어 있는 상태로만
> 테스트됐다. 아래 "VWP 실기에서 확인이 필요한 것들"을 먼저 볼 것.

1. **계정 성격** — `Account.kind` (프로젝트 / 데이터 백업 / 미지정) +
   프로젝트 계정의 `backup_account_id`. 계정 관리 표에서 콤보로 바로 고치고,
   대시보드에도 `성격` 열이 생겼다.
2. **과제 생성 감지** — 이름에 `_run_`이 든 디렉터리가 직전 세대에 없다가
   생기면 일간 보고서 `[과제 생성]`에 나온다. **추가 I/O 없음** - 야간
   `du -k`가 이미 `baseline_results`에 넣어 둔 경로의 세대 차이만 본다
   ([workflow.py](smvwp/workflow.py)). 프로젝트 계정에서만 본다.
3. **리소스 시계열** — `scan_load_samples` 표에 스캔 전/중 표본을 주기(기본
   30초)로 남기고 일간 보고서 `[스캔 중 리소스 변화]`에 요약한다. iowait과
   load를 CPU보다 위에 놓았다 (`du`는 CPU가 아니라 I/O 대기를 만든다).
4. **계정 병렬 실행** — 평일 밤은 직렬(`nightly_parallel_accounts`, 기본 1),
   **주말 밤은 동시 3개**(`weekend_parallel_accounts`). `--parallel N`이 둘 다
   이긴다. 주말 판정은 날짜가 아니라 **끝나는 아침** 기준이라 금요일 밤은
   주말이고 일요일 밤은 평일이다 (`scan_window.ends_on_weekend`).
   낮에 손으로 돌리는 실행에는 주말 규칙을 적용하지 않는다.
5. 보고서 표를 표시 폭 기준으로 정렬 (`reports.pad`) — 한글이 두 칸을
   차지해 기존 계정 표도 헤더와 값이 어긋나 있었다.

## 바로 다음에 할 것: BACKUP 중복 백업 대조

사용자가 예고한 기능이다. **데이터 모델은 이미 준비돼 있다.**

- 프로젝트 계정의 `<과제>/LAYOUT/<NN_run_MMDD>/BACKUP` 1레벨 하위 내용이
  연결된 백업 계정(`Account.backup_account_id`)에 들어가 있는지 대조한다.
- 들어가 있으면 **중복이므로 프로젝트 쪽을 지워도 된다**고 보고한다.
- 필요한 조각은 다 있다: `workflow.backup_dir_for()`,
  `scan_store.generation_paths(under=...)`, `config.backup_accounts()`.
- **주의**: [reports.py](smvwp/reports.py) 모듈 docstring의 대원칙 —
  자동 삭제 없음. "지워도 될 것 같은 후보"를 나열만 한다.
- **주의**: 이름만 같고 내용이 다를 수 있다. 크기까지 같아야 후보로 올리고,
  확신이 없으면 올리지 않는 쪽을 택한다 (`build_cleanup_candidates`의 3조건이
  같은 태도로 쓰여 있으니 톤을 맞출 것).

## 실기(RHEL)에서 해야 할 실측

병렬 이득 여부는 **재 보기 전에는 알 수 없다.** 이제 재는 도구가 생겼으므로:

```
./smvwp_cli.py scan --now --parallel 1
./smvwp_cli.py scan --now --parallel 4
```

두 밤의 일간 보고서 `[스캔 중 리소스 변화]`를 비교한다. 이 결과가 아래 로드맵
3번(파일시스템 단위 병렬)을 할지 말지를 정한다 — 계정들이 같은 볼륨에 몰려
있으면 병렬은 seek 경합만 늘리므로 안 하는 것이 답일 수 있다.

**주말 기본값 3은 측정 전의 보수적 출발점이지 최적값이 아니다.** 한 주 돌려
보고 `weekend_parallel_accounts`를 올리거나 내린다. 주말에도 10~20%는 출근하므로,
그 사람들의 job이 이 프로그램 때문에 느려지면 본말이 전도된다는 점이 상한을
정하는 근거다.

### 공휴일은 아직 주말로 안 본다

설날·추석 연휴는 주말과 같은 상황(사람이 거의 없음)인데 지금은 평일로 판정한다.
넣는다면 `config.json`에 날짜 목록(`extra_idle_dates` 같은 것)을 두고
`scan_window.ends_on_weekend`가 그 날짜도 보게 하는 정도면 충분하다 — 폐쇄망이라
공휴일 API는 못 쓴다. **사용자에게 아직 제안만 하고 안 만든 상태다.**

## gdu 활용 (논의만 됨, 미착수)

사용자가 gdu를 설치했다. 확인 결과 쓸 만한 자리는 **스캔 백엔드 교체가 아니라
뷰어**다.

- **추천**: `baseline_results`를 ncdu 호환 JSON으로 내보내고 `gdu -f`로 연다.
  로드맵 2번(트리 뷰어)을 Qt 화면 없이 대부분 해결하고, **디스크를 다시 안
  건드린다**(낮에도 어젯밤 결과를 파고들 수 있다). 권한 논의의 2번 선택지
  ("담당자가 산출물을 떨궈 두고 viewer는 그것만 본다")와도 맞물린다.
  - 구현 시 함정 두 가지: (a) ncdu는 부모 크기를 자식 합으로 계산하므로
    `max_depth`에서 잘린 곳에 `부모 - 자식합`을 "(하위 미측정)" 합성 항목으로
    넣어야 총계가 맞는다. (b) 경로를 raw 문자열로 정렬하면 `/a` → `/a-b` →
    `/a/b` 순이라 중첩이 깨진다. `/`로 쪼갠 튜플로 정렬할 것.
- **비추천**: 야간 `du -k`를 gdu로 교체. gdu는 트리를 메모리에 다 만든 뒤 JSON을
  쓰므로 **시간 초과 시 부분 결과가 통째로 사라진다** —
  [detail_scan.py](smvwp/detail_scan.py)의 `DuTreeOutcome` docstring이 설명하는
  현재 설계의 핵심 성질이 깨진다.

## 미결: 사내 공용 배포 구조 (사용자 승인 대기)

### 확정된 환경

- 서버 한 대에 10명 내외 로그인, 스캐닝 담당자는 2~3명, 계정 6~10개.

### 검토 결과

- **야간 스캔은 사람 단위가 아니라 서버 단위 작업이다.** `scan_lock`이 전체
  실행을 하나만 허용하므로, 담당자마다 cron을 걸면 먼저 잡은 한 명만 돌고
  나머지가 담당하는 계정은 그날 스캔되지 않는다. cron은 서버에 **하나만**.
- `Account.owner`(담당자)는 스캔 분담용이 아니라 "누구에게 물어보나"용이다.
  `created_by`("등록한 사람")와 뜻이 다르므로 별도 필드가 맞다. **아직 미구현.**
- 담당자와 일반 사용자를 가르는 실제 경계는 **데이터 디렉터리 쓰기 권한**
  하나뿐이다. 실행 파일 분리는 강제가 아니라 안내다. README에 같은 톤으로
  명시해야 한다.

### 실측으로 확인한 제약

**SQLite를 `mode=ro`로 열어도 `-wal`/`-shm`이 생성된다.** 즉 읽기 전용
사용자에게도 데이터 디렉터리 쓰기 권한이 필요하다. 선택지는 (1) `770`으로 두고
강제는 포기, (2) 산출물만 별도 경로에 떨궈 viewer가 DB를 아예 안 열게 하기.
위 gdu 뷰어 안이 (2)에 해당한다.

NFS는 별개로 더 위험하다 — WAL이 동작하지 않으므로 데이터 디렉터리는 **반드시
서버 로컬 디스크**여야 한다. README에 이 경고가 아직 없다.

### 남은 작업 순서

1. `Account.owner` 추가 + 계정 표에 `담당자` 열
2. `smvwp_cli.py view` — 읽기 전용 진입점 (스케줄러를 띄우지 않음).
   지금은 GUI를 열면 무조건 스케줄러가 뜬다 ([main_window.py](smvwp/gui/main_window.py))
3. cron 중복 등록 감지 (`setup_cron.csh`)
4. 권한 모델 문서화 (강제 vs 안내 구분 + 로컬 디스크 필수 경고)

## 그 밖의 로드맵

| # | 항목 | 상태 |
|---|---|---|
| 1 | `du -k` 단일 순회로 전환 | **완료** |
| 2 | 트리 뷰어 (ncdu/gdu 형태) | 미착수 — 위 gdu 절 참고 |
| 3 | 파일시스템 단위 병렬 | 계정 단위 병렬은 실측 옵션으로 들어감. 실기 실측 후 판단 |
| 4 | 증분 스캔 (mtime 기반) | 미착수 |

**4번 증분 스캔 주의점**: 파일이 제자리에서 커지면 상위 디렉터리 mtime은 바뀌지
않는다(로그 append가 정확히 이 경우). 반드시 **파일 단위** `find -newermt`로
판정해야 한다.

## VWP 실기에서 확인이 필요한 것들

- `du -k --max-depth` / `--exclude`가 실제 RHEL `du`에서 기대대로 도는지
- 창을 닫을 때 자식 프로세스 정리 (`/proc` 기반이라 리눅스에서만 실제 동작)
- **리소스 시계열이 실제로 어떻게 찍히는지** — 개발 PC(Windows)에는 `/proc`이
  없어 값이 전부 비어 있다. `phase='before'` 행이 남는 것까지만 확인했다.
- 99% 즉시 알림 팝업 (`config.json`의 `immediate_notify_pct`를 낮춰 시험)
- 일간 보고서가 매일 생성되는지 (`<data_dir>/reports/daily/`)
- **과제 생성 감지가 실제 디렉터리 구조에서 걸리는지** — 계정 루트가 과제명
  바로 위여야 기본 `detail_scan_max_depth=3`으로 run 디렉터리가 잡힌다.
  안 잡히면 깊이를 올려야 한다.

## 알아 둘 함정 (작업하다 걸렸던 것들)

- **Bash heredoc이 깨진다.** 파이썬 코드를 heredoc으로 넘기면 따옴표/이스케이프가
  어긋나 `unexpected EOF`가 난다 (이번에도 또 걸렸다). **파일에 써서
  `python <파일>`로 실행하거나 `cat <파일> >> 대상`으로 붙이는 것이 안전하다.**
- **`git stash --keep-index`가 인덱스를 흐트러뜨렸다.** 부분 스테이징이 필요하면
  "원하는 상태의 파일 내용을 직접 써서 `git add` → 원래 내용 복원"이 더 안전하다.
- **Qt 내장 정렬은 칸 위젯과 함께 쓰면 안 된다.** 항목만 옮기고 위젯은 제자리에
  남아 행과 어긋난다. 계정 관리 표(성격·연결 콤보가 칸 위젯)는 정렬을 켜지 않는다.
- **칸 위젯이 든 표를 다시 그릴 때는 `setRowCount(0)`으로 비운다.**
  `clearContents()`는 항목만 지우고 칸 위젯은 남겨 이전 계정의 콤보가 살아남는다.
- **신호 처리 중에 그 신호를 낸 위젯을 지우면 크래시한다.** 콤보 변경으로 표를
  다시 그려야 하면 `QTimer.singleShot(0, ...)`으로 이벤트 루프에 넘긴다.
- **QSS로 `::item`을 스타일링하면 `setBackground()`가 무시된다.** 행 배경색은
  델리게이트에서 직접 칠한다 (`widgets.TierRowDelegate`).
- **SQL `LIKE`에서 `_`는 한 글자 와일드카드다.** `_run_`을 찾으려면
  `LIKE '%\_run\_%' ESCAPE '\'`로 이스케이프해야 한다. 안 하면 `xrunY`도 걸린다.
- **한글은 고정폭 터미널에서 두 칸이다.** `f"{x:<18}"`은 글자 수로 세므로 표가
  어긋난다. 보고서 표는 `reports.pad()`를 쓴다.
- **야간 작업의 요일 판정은 "지금"이 아니라 "끝나는 아침"으로 한다.** 스캔이
  자정을 넘어가므로 오늘 요일로 판정하면 금요일 밤과 일요일 밤이 정확히
  뒤집힌다. 테스트를 쓸 때도 날짜의 실제 요일을 확인할 것 — `2026-08-22`는
  토요일이라 "기본은 직렬" 테스트가 주말 규칙에 걸려 깨졌다.
