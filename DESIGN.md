# 설계와 구현 이력

설치·운영 방법은 [README.md](README.md)를 보세요. 이 문서는 "왜 이렇게
만들었는가"만 다룹니다.

문서가 셋으로 나뉘어 있던 것을 하나로 합쳤습니다. 폐쇄망에는 파일 하나하나가
반입 대상이라 굳이 나눠 둘 이유가 없고, 세 문서 모두 같은 질문("왜 이 선택을
했는가")에 답하고 있었기 때문입니다. 아래 순서는 실제 작업 순서와 같습니다:
기존 구현에서 컨셉을 뽑고 -> 재구현 방향을 정하고 -> 그대로 구현했습니다.

---

## 1부. 기존 구현에서 뽑은 설계 철학

> 이 문서는 기존 `storage_manager_vwp` 저장소(현재 로컬 클론, origin:
> https://github.com/jiohz5/storage_manager_vwp)의 **설계 컨셉만** 정리한 것입니다.
> 기존 Python/PyQt5 코드를 그대로 옮기지 않고, 새로운 작업(다른 스택/구조 포함)을
> 시작할 때 참고할 "왜 이렇게 설계했는가"를 남기는 것이 목적입니다.

### 1. 문제 정의 & 제약

- **폐쇄망 RHEL/VWP 환경**: 인터넷 연결 없음, 사내 반입 절차로만 배포 가능. 외부
  패키지·wheel 설치가 기본적으로 불가 → 사내에 이미 있는 Python/Qt만으로도
  돌아가야 한다. (뒤에 사내 프록시로 pip이 되는 곳이 있음이 확인되어, GUI는
  Fluent를 우선 쓰되 없으면 PyQt5로 폴백하도록 바꿨다. 백엔드·cron 경로는
  여전히 표준 라이브러리만 쓴다.)
- **다중 프로젝트 계정 스토리지 모니터링**: 여러 계정(예: `/user/project_a`)의
  용량/inode 사용률을 감시해 FULL 상태를 사전에 경고.
- **읽기 전용 원칙**: 모니터링 대상 계정에는 절대 쓰거나 삭제하지 않음. 유일한
  예외는 사용자가 명시적으로 지정한 전용 데이터 저장 경로.
- **공유 파일시스템 위에서 동작**: `df`는 계정 자체 사용량이 아니라 그 경로가
  속한 파일시스템 전체 사용량이라는 점을 UI/문서에서 항상 명시.

### 2. 핵심 설계 철학

1. **자동 삭제 없음** — 정리 후보 보고서만 생성하고 실제 삭제 명령은 절대 실행하지
   않는다. 신뢰를 지키는 것이 기능 추가보다 우선.
2. **중단 가능·재개 가능(resumable)** — 모든 무거운 순회 작업(`du`, `find`,
   검색 인덱싱)은 디렉터리 단위 체크포인트를 남기고, 시간창이 끝나거나 안전
   중지되면 다음 실행에서 그 지점부터 이어간다. "처음부터 다시"가 없다.
3. **부하에 대한 겸손함** — 관측 대상 스토리지 서버가 아니라 "이 클라이언트에서
   본" load/PSI만 근거로 삼고, 그 사실을 항상 명시한다. `nice`/`ionice`는
   우선순위 조정일 뿐 처리량의 절대 상한이 아니라는 점도 문서화한다.
4. **안전한 중지, 강제 kill 없음** — 임의 PID에 signal을 보내지 않는다. 실행별
   run ID가 일치하는 요청 파일만으로 중지하고, 실행 중인 하위 프로세스만 정리한다.
   (과거 PID 재사용으로 무관한 프로세스를 죽일 뻔한 리스크가 이 설계의 배경.)
5. **자체 비대화 방지** — 모니터링 프로그램 자신도 스토리지를 잡아먹지 않도록
   DB 크기 예산을 처음부터 설계에 포함한다 (일 단위 upsert, 보존 기간, 로그 로테이션).
6. **UI 위기감 과장 금지** — 진행률/퍼센트는 추정치일 때 "약", "현재 발견 기준"
   같은 표기를 명확히 붙이고, 완전히 끝난 순회에만 정확한 100%를 표시한다.

### 3. 시간 기반 실행 모델

| 구간 | 성격 | 대상 |
|---|---|---|
| 00:00~24:00 (15분 간격) | 초경량 `df`/inode/quota 표본만, `du`/`find` 없음 | 모든 활성 계정 |
| 22:00~06:00 (야간) | 전체 상세 스캔(`du`, `find`, 검색 인덱싱) | 모든 활성 계정, 직렬 처리 |
| 06:00~22:00 (주간, 로드맵 진행 중) | 짧고 재개 가능한 저부하 분석, 계정 1개씩 | 급증/고사용률/미완료 우선 계정 |
| 07:00 | 건강 점검 | 수집 누락/실패/인계 실패 감지 |

- 야간 전체 스캔은 06:00에 **완료된 체크포인트를 남기고 `paused`로 종료** — 강제
  종료가 아니라 "안전하게 멈춤"이라는 상태를 구분해서 보여준다.
- 로드맵의 "적응형 주간 분석"은 22:00~06:00 외 시간을 완전히 놀리지 않고, 대신
  1분 load average·`/proc/pressure/io` 기준으로 부하가 낮을 때만 짧게(예: 매시간
  10분) 개별 계정을 분석하는 개념. 21:55에는 항상 야간 작업에 실행권을 넘긴다
  (hand-off).
- 터미널에서 사람이 직접 실행하는 스캔은 시간창 제한을 받지 않는다 — 이것은
  "의도적 진단/복구 경로"로 별도 취급.

### 4. 데이터 및 경고 모델

- **사용률 등급**: 90% 미만 정상 / 90~94% 주의 / 95% 이상 경고 / 98% 이상 긴급
  / 100% FULL. byte와 inode를 독립적으로 평가 (inode 100%는 용량이 남아도 파일
  생성을 막을 수 있음).
- **급증 탐지**: 최근 15분 표본의 원시 KB 증가율로 "N시간 내 FULL 예상" 경고.
  통계 이상탐지는 최근 양의 증가량 중앙값 + MAD 기반, 최소 절대값 임계치(예:
  100GB) 동반.
- **추세/예측**: 7일·30일 기울기로 FULL 예상일 계산. 표본 부족, 감소/정체 추세,
  10년 초과 예상은 "예측 불가"로 명시 — 억지로 숫자를 만들지 않는다.
- **정리 후보(cleanup candidates)**: 용량 임계치 + 최초 관찰 후 경과일 + 최근
  활동 없음, 3개 조건을 모두 만족해야 후보로 표시. 삭제는 절대 실행하지 않음.
- **DB 크기 예산을 설계 초기부터 문서화**: 계정 수·보존기간별 예상 크기를 표
  형태로 관리해 "모니터링 툴이 스토리지를 잡아먹는" 역설을 방지.

### 5. 알림 아키텍처

폐쇄망이므로 알림은 로컬/사내로만 국한:

- **파일 outbox (기본)** — cron이 JSON을 파일로 쌓고, 별도 트레이 프로세스가
  독립적으로 읽어 팝업 표시. GUI 종료와 무관하게 동작.
- **사내 command / 내부 webhook** — 선택적. shell 없이 고정 argv 실행 또는
  내부 HTTPS POST만 허용 (외부 인터넷 금지 원칙 유지).
- **감사 로그**: 어떤 모드든 알림 원문과 전송 결과를 데이터 디렉터리에 남긴다.
- **cooldown**: 동일 key·동일 등급은 일정 시간 억제하되, 심각도가 상승하면
  즉시 재알림. 정상 복귀 후에는 주기를 리셋.
- **메인 창과 notifier 프로세스를 분리**: 관리 GUI를 닫아도(최소화 개념) 알림은
  계속 살아있어야 한다는 요구가 구조를 결정했다.

### 6. 검색(관리자 전용) 기능 컨셉

- 파일 내용이 아니라 이름/확장자/경로만 별도 인덱스에 저장 — 목적은 "어디 있는지
  찾기"이지 내용 검색이 아님.
- 계정별 opt-in — 기본은 꺼짐, 필요한 계정만 인덱싱.
- 접근 제어는 **UI 노출 제한**(PIN)이지 OS 권한이나 암호화가 아니라는 점을
  명시 — 실제 보안 경계가 아니라 실수 방지용 장치라는 것을 오해하지 않도록.
- 인덱스도 디렉터리 체크포인트 기반으로 재개 가능해야 하고, 삭제/rename은
  주기적 전체 대조로만 정리(매일 완전 재인덱싱은 하지 않음).

### 7. 대용량 순회에 대한 안전장치 (일반화 가능한 패턴)

- 먼저 전체 계정의 가벼운 지표(`df`)부터 수집 → 그 다음에 느린 상세 작업 →
  한 계정이 느려도 다른 계정 보고가 막히지 않는다.
- 디렉터리 단위 타임아웃 → 넘으면 하위 디렉터리로 분할해 재시도.
- 여러 계정의 상세 작업은 동시에 두 개 이상 돌리지 않고 직렬 처리 (공유
  파일시스템에 부하를 몰아주지 않기 위함).
- 완료 순서를 날짜별로 순환시켜 특정 계정이 계속 뒤로 밀리지 않게 공정성 확보.
- 완료 직전 top-level 재대조로 순회 중 생성/삭제된 항목 반영.
- 기준선/커서는 "DB에 기록한 뒤에만" 체크포인트를 지운다 — 중단 시점이 언제든
  재실행이 항상 안전해야 한다는 불변식.
- 특수 디렉터리(`.snapshot` 등 스냅샷/백업 산출물)는 계정 루트 바로 아래에서만
  제외하고, 그 아래 중첩된 동일 이름 경로는 일반 데이터로 취급.

### 8. 모듈 구성 (기존 구현 참고용 — 역할 매핑)

새로 설계할 때 "어떤 책임 영역이 있었는지"를 참고하는 용도로만 나열합니다
(코드 자체를 옮기라는 뜻이 아님):

- 계정/경로 정책, 인증(관리자 PIN)
- 수집기(df/inode/quota) — 경량 주기 작업
- 재개 가능한 기준선 스캔(`du` 기반) / 변경 파일 활동 스캔(`find` 기반)
- 검색 인덱스
- 분석/추세/이상탐지
- 리포트 생성(일간/주간/정리 후보), 다국어(KOR/ENG)
- 알림 구성(outbox/command/webhook) + 큐 + notifier 프로세스
- 스케줄러 / 실행 시간창 관리
- 헬스체크(수집 누락 감지)
- GUI (대시보드, 추적/분석 탭)
- 런타임 진단 (Python/Qt/SQLite/권한 사전 점검)

### 9. 아직 열려있는 확장 아이디어 (기존 문서 기준)

- 파일시스템이 snapshot diff/changelog API를 제공하면 전체 `find` 순회 대체 가능
  (대용량 환경 최대 성능 개선 후보).
- 여러 관리자가 이력을 공유해야 하면 로컬 SQLite 공유 대신 중앙 수집기 + 읽기
  전용 GUI/API 구조로 전환.
- 역할 기반 권한, 계정 편집/cron 변경/중지 요청 감사 로그.
- 가상 시나리오(30/60/90일 예상 신규 데이터량 추가) 용량 시뮬레이션.
- 담당자/과제명/계정 생명주기 메타데이터는 기존 범위에서 의도적으로 제외됨
  (요청에 따라).

---

이 문서는 "왜"와 "무엇을 지켜야 하는가"에 집중했고, 실제 구현 방식(언어/프레임워크/
파일 구조)은 새 작업에서 자유롭게 다시 결정할 수 있도록 의도적으로 비워두었습니다.

---

## 2부. 재구현 결정 사항

> 1부에 정리한 기존 설계 철학을 **개념적으로만** 계승하고,
> 코드는 완전히 새로 작성합니다. 목표는 "VWP 폐쇄망에서 Python 3.12 경로 하나만
> 잡아주면 `run.csh`로 바로 뜨는" 배포 경험입니다.

### 1. 이번 재구현의 목표

- 기존 구현(Python 3.10 가정, PyQt5, 다수 모듈)의 **아이디어만 재사용**하고
  코드/구조는 그대로 옮기지 않는다.
- 설치·설정 단계를 최소화한다: 사용자가 하는 일은 **Python 3.12 실행 파일 경로
  지정 하나** 뿐이어야 한다.
- `run.csh`가 유일한 실행 진입점이다. 실행 즉시 앱이 뜨거나, 문제가 있으면
  무엇이 잘못됐는지 명확히 알려준다.

### 2. 배포/실행 계약 (변하지 않는 제약)

- 폐쇄망 RHEL/VWP, 인터넷 접근 없음, 외부 패키지 설치가 기본적으로 불가 → 사내에 이미 설치된
  Python 3.12 + (필요 시) PyQt5만 사용.
- 소스 배포본은 Python 런타임/wheel을 포함하지 않는다 (가벼운 반입 파일 유지).
- 데이터 저장 경로는 모니터링 대상과 분리된, 쓰기 가능한 별도 위치.

### 3. `run.csh` 단순화 방향 (결정됨)

기존에는 `STORAGE_MANAGER_PYTHON_BIN` / `STORAGE_MANAGER_PYTHON_HOME` 두 가지
방식을 다 지원했는데, 신규 버전은 **`bin/python3` 실행 파일 경로 하나만** 받는다.
`_HOME`(설치 prefix) 방식은 지원하지 않는다 — 옵션이 하나면 사용자가 헷갈릴
여지가 없다.

- 사용자는 **Python 3.12 실행 파일 경로 하나**만 환경변수로 지정 (예:
  `STORAGE_MANAGER_PYTHON_BIN=/installed/python/3.12.x/bin/python3`).
- `run.csh`는 그 경로가 실제 실행 가능한지, 버전이 3.12인지, 필요한 표준
  모듈(`json`, `sqlite3` 등)과 (사용한다면) PyQt5 import가 되는지 먼저 확인한
  뒤 바로 앱을 실행한다 — 진단과 실행이 한 번의 호출로 끝나는 느낌.
- 상속된 `PYTHONHOME`은 여전히 경고 후 제거 (Python 표준 라이브러리 경로 꼬임
  방지라는 기존 교훈은 유지).
- 데이터 저장 경로는 최초 실행 시 한 번 GUI(또는 CLI 프롬프트)에서 지정하고,
  이후에는 작은 포인터 파일로 기억 — 매번 경로를 다시 넣지 않게 하는 기존
  컨셉은 유지.

### 4. Python 3.12로 올리면서 재검토할 것

- 설정 파일 포맷은 **JSON으로 결정** (stdlib만으로 읽기/쓰기 모두 간단, 기존과
  동일한 방식이라 검증된 경로 유지). `tomllib`(3.11+)은 검토했으나 채택하지 않음.
- PyQt5 자체 호환성은 실제 VWP의 Python 3.12 배포판에서 별도 검증 필요 —
  진단 스크립트에 반드시 포함.
- 기존 RHEL 커맨드 의존성(`df`, `du`, `find`, `crontab`, `csh`, 선택적
  `nice`/`ionice`)은 변하지 않으므로 그대로 승계.

### 5. 계승할 설계 원칙 (1부 요약 참조)

새 코드베이스에서도 반드시 지킬 불변식들 — 자세한 배경은 1부:

1. 모니터링 대상에는 절대 쓰기/삭제하지 않는다 (전용 데이터 경로 제외).
2. 자동 삭제 없음 — 정리 후보는 보고서로만.
3. 무거운 순회는 전부 재개 가능(resumable), 중단 시점 상관없이 안전.
4. 부하 판단은 "이 클라이언트에서 관측한 값"이라는 한계를 항상 명시.
5. 강제 kill 없음 — run ID 매칭 방식의 안전 중지만 사용.
6. 모니터링 툴 자신이 스토리지를 잡아먹지 않도록 DB 크기 예산을 처음부터 설계.
7. 추정 퍼센트/예측 불가 표기 등 "과장하지 않는 UI" 원칙 유지.

### 6. GUI 방향 (결정됨) — 컨셉만 남기고 완전히 새로 설계

기존 PyQt5 GUI의 화면 구성/탭 구조/위젯 배치를 그대로 옮기지 않는다. 유지하는
것은 **"무엇을 보여줘야 하는가"라는 개념**뿐이고 (계정별 사용률, 등급, 추세,
스캔 진행상태, 알림 상태 등), 실제 화면 설계·레이아웃·상호작용은 처음부터
다시 그린다.

- 프레임워크는 폐쇄망에서 이미 검증된 PyQt5를 우선 가정하되, 다른 대안이
  필요하면 실제 구현 착수 전에 다시 논의한다.
- 기존 문서(REVIEW.md 등)에 남아 있던 운영 히스토리성 UX(예: X 버튼 최소화,
  관리자 PIN 검색 노출 방식)는 "그런 요구가 있었다"는 참고 정보일 뿐, 신규
  화면 설계를 그 모양대로 따라가야 한다는 뜻은 아니다.
- **화면 구조: 대시보드 단일 화면**으로 결정. 계정 목록·요약·상태를 한 화면에
  모으고, 계정 등록/설정처럼 자주 쓰지 않는 동작만 다이얼로그로 분리한다.
  기존처럼 여러 탭(Accounts/Dashboard/추적/분석 등)으로 나누지 않는다.

#### GUI 사용성 개선 방향

기존 설계의 알려진 불편함을 참고해 제안했던 개선 목록이며, 1차 범위에서는
**"한눈 요약 우선"만 채택**한다. 나머지는 1차 범위 이후 후보로 남겨둔다.

- **[1차 채택] 한눈 요약 우선**: 대시보드 최상단에 "경고 이상 계정 수 / 가장
  급한 계정" 요약을 배치해, 표를 스크롤/정렬하지 않아도 즉시 위험 상태를 파악.
- [이후 후보] 최초 실행 마법사: 첫 실행 시 계정 등록 → 데이터 경로 지정 →
  진단 확인을 단계별로 안내.
- [이후 후보] 상태를 한 곳에서: 스캔 진행률/cron 상태/알림 상태를 한 화면에
  통합 (단일 화면 결정과 상당 부분 자연스럽게 겹침 — 1차 구현 시 재검토).
- [이후 후보] 색상 + 텍스트 라벨 병행: 사용률 등급을 색상만이 아니라 텍스트
  라벨로도 항상 표기해 접근성 확보.
- [이후 후보] 클릭 수 최소화: 자주 쓰는 동작(진단, 지금 스캔, 알림 테스트)을
  대시보드에서 바로 실행 가능하게.

### 7. 1차 범위 (결정됨 — 그대로 유지)

빠르게 "python3.12 경로 지정 → run.csh → 바로 동작"을 검증하기 위한 최소 범위:

1. 계정 등록 + `df` 기반 byte/inode 대시보드
2. 사용률 등급(정상/주의/경고/긴급/FULL) 표시
3. 15분 주기 경량 수집 (cron 또는 백그라운드 타이머)
4. 로컬 파일 outbox 알림 (가장 단순한 알림 경로부터)
5. `run.csh --diagnose`에 준하는 최소 진단(Python 버전, 필요 모듈, 데이터 경로
   쓰기 권한)
6. 대시보드 단일 화면 구조 + "한눈 요약 우선" UX를 1차 범위 화면 설계에 반영

야간 상세 스캔, 검색 인덱스, quota 연동, 다국어, 정리 후보 보고서 등은 1차 범위
밖에 두고 이후 단계에서 순차 추가한다.

### 8. 1차 범위 이후 확장 순서 (결정됨)

1차 범위(7번) 완료 후 다음 순서로 기능을 추가한다:

1. **야간 상세 스캔** (`du`/`find` 기반 계정별 증가 경로 분석) — 용량 문제의
   원인 파악에 가장 임팩트가 크므로 최우선.
2. 이후: 알림 채널 다양화(command/webhook), 관리자 검색 기능, 나머지 GUI
   사용성 개선 후보(6번), quota 연동, 다국어, 정리 후보 보고서 등은 야간 상세
   스캔 이후 순서를 다시 논의해 정한다.

### 9. 다음에 정할 것 (남은 열린 질문)

- 모듈 분리 방식, 파일 구조 (신규 코드베이스의 실제 디렉터리 레이아웃).
- 야간 상세 스캔을 1차 범위 스케줄(15분 수집)과 어떻게 통합할지 세부 설계
  (기존의 22:00~06:00 시간창 정책을 그대로 가져올지, 처음부터 다시 설계할지).

이 문서는 실제 구현을 시작하기 전 컨셉 정렬용이며, 위 열린 질문에 답이 정해지면
그에 맞춰 갱신합니다.

---

## 3부. 구현 결과

> 1·2부는 **작업 당시의 판단 기록**이라 그대로 둔다. 그 사이 사내 프록시로
> pip이 된다는 것이 확인되어 GUI를 PyQt6 Fluent로 옮겼다가, 사내 저장소의 Qt가
> 6.9라 `libxcb-cursor`(시스템 라이브러리, pip 불가)가 필요해지는 것을 보고
> **되돌렸다.** 설치 마찰을 없애자던 원래 제약을 툴킷 교체가 깨뜨렸기 때문이다.
> 지금은 이미 깔려 있는 **PyQt5**를 쓰고 외양은 자체 QSS 테마로 해결한다.
> 아래는 **현재 상태**다.

### 디렉터리 구조

```
storage_manager_vwp/
  run.csh              # 단일 진입점 (진단 + 실행)
  setup_cron.csh        # 15분 주기 수집 cron 등록 도우미
  smvwp_cli.py          # 단일 진입점 (gui/collect/scan/notify)
  smvwp/                  # 애플리케이션 패키지
    tiers.py               # 사용률 등급 계산 (정상/주의/경고/긴급/FULL)
    paths.py                 # 데이터 디렉터리 탐색/기억/쓰기 안전성
    config.py                 # JSON 설정 (계정 목록 + 전역 설정)
    collector.py               # df/inode(+quota) 조회 및 파싱
    store.py                    # SQLite 표본 저장소 (보존기간 정리 포함)
    quota.py                     # 사내 quota JSON 어댑터 (선택)
    notifications.py              # 알림 생성 + outbox/command/webhook 전송 + 감사
    popup_queue.py                 # outbox 읽음 상태 관리 (Qt 비의존)
    notifier.py                     # 트레이 알림기 + XDG autostart
    cycle.py                         # 수집->저장->알림 한 사이클 (GUI/cron 공유)
    analytics.py                      # FULL 도달 예측 (최소제곱 회귀)
    forecast_notify.py                 # 예측/급증 알림 (파일시스템 단위 중복제거)
    procio.py                           # 외부 명령 UTF-8 입출력 래퍼
    i18n.py                            # 한국어/영어 문자열 카탈로그
    reports.py                          # 일간/주간/정리 후보 보고서
    readability.py                       # 계정 경로 읽기 권한 표본 조사
    diagnostics.py                        # Python/모듈/GUI툴킷/데이터 디렉터리 진단
    scan_window.py                         # 22:00~06:00 시간창 계산
    scan_lock.py                            # 실행 잠금 + run ID 매칭 안전 중지
    scan_store.py                            # 스캔 체크포인트/기준선 SQLite
    detail_scan.py                            # du 기반 기준선 스캔 (분할/우선순위)
    activity_scan.py                           # find -newermt 기반 변경 파일 스캔
    nightly_scan.py                             # 야간 스캔 오케스트레이터
    search_index.py                              # 이름 검색 인덱스 (별도 DB)
    admin_auth.py                                 # 관리자 PIN (UI 노출 제한)
    scheduler.py                                   # GUI 내부 타이머 (cron 부재 대비)
    loadstat.py                                     # /proc 기반 CPU 점유 측정
    gui/                                            # PyQt5 화면 (Qt 의존)
      fonts/                                         # 번들 폰트 (Pretendard, OFL 1.1)
      theme.py                                        # 폰트·팔레트·타이포·QSS
      main_window.py                                  # 대시보드 단일 화면
      account_dialog.py                                # 계정 등록/설정
      reports_dialog.py                                 # 보고서 보기/생성
      search_dialog.py                                   # 관리자 검색
      pin_dialog.py                                       # 관리자 PIN 변경
      scan_progress_dialog.py                              # 스캔 진행 상황 (읽기 전용)
      first_run.py                                          # 최초 실행 안내
      widgets.py                                            # 등급 배지 / 크기 표기
  tests/                # unittest 기반 단위 테스트 (Qt 불필요)
    support.py            # 공용 테스트 헬퍼 (bytes stdout, 단일 명령 러너)
```

**모듈 분리 원칙**: `smvwp` 최상위와 `gui/`, `scheduler.py`를 의도적으로
나눴다. `tiers`/`paths`/`config`/`collector`/`store`/`notifications`/`cycle`/
`diagnostics`는 Qt를 전혀 import하지 않으므로, Qt가 없는 환경(예: 이
저장소를 검토만 하는 개발 PC, 또는 cron 환경)에서도 그대로 단위 테스트하고
재사용할 수 있다. Qt 의존은 `smvwp/gui/*`와 `smvwp/notifier.py`의 트레이
부분에만 있다.

이 구조는 이번 작업에서 임의로 정한 것이며 (2부 9절
"모듈 분리 방식은 아직 열린 질문"), 다음 세션에서 사람이 바꾸기 쉽도록 각
모듈의 책임을 최대한 좁게 나눠 두었다.

### 화면 외양 (테마)

"화면이 초보가 만든 것 같다"는 지적에서 출발해, 원인을 QSS 문법이 아니라
**폰트·타이포 위계·여백·정보 배치** 네 가지로 보고 그쪽을 손봤다. 기성 테마
패키지(qdarkstyle 등)를 얹는 방법도 있지만, 그것들은 색만 바꿀 뿐 위 네 가지를
건드리지 않아 같은 인상이 남는다.

- **폰트를 번들한다.** Pretendard Regular/Bold/ExtraBold를 `smvwp/gui/fonts/`에
  두고 `QFontDatabase.addApplicationFont`로 싣는다. 시스템 한글 폰트는 장비마다
  다르고 굵기 단계가 빈약해 무엇을 크게 써도 위계가 생기지 않는다.
  - 가변폰트 한 벌이면 파일이 하나로 끝나지만 **Qt 5는 가변폰트를 Regular/Bold
    두 단계로만 읽는다** (실측: weight를 Medium/Black으로 줘도 렌더가 Normal/
    Bold와 픽셀 단위로 동일). 정적 세 벌이 파일도 더 작고(4.5MB vs 6.4MB)
    굵기도 세 단계 나온다.
  - 굵기는 반드시 weight로 고른다. `setStyleName("Bold")`은 이 조합에서 Regular로
    떨어진다.
- **타이포 스케일을 못박았다** (30/20/15/14/12). 그때그때 "조금 더 크게"를
  정하면 화면마다 값이 달라진다.
- **히어로를 뒀다.** 가장 높은 사용률을 큰 숫자로 먼저 보여준다. 예전에는 요약
  문장 한 줄뿐이라 정작 궁금한 값이 본문과 같은 크기로 묻혔다.
- **표 열 폭을 열마다 다르게 준다.** 전부 Stretch로 두면 열 개수로 균등 분배돼
  값이 짧은 열(`파일시스템`='C:')이 공간을 낭비하는 동안 `경로`는 잘려 아무것도
  안 보인다 (실측: 경로에 364px 필요, 균등 분배로는 166px). 사용률은 숫자 옆에
  막대를 함께 그려 계정 간 비교가 눈으로 되게 했다.
- **행 배경색은 델리게이트가 그린다.** QSS로 `::item`을 스타일링하면 Qt가
  `QTableWidgetItem.setBackground()`를 무시한다. 두 방식이 충돌하지 않도록
  `widgets.TierRowDelegate`에서 직접 칠한다.
- **그림자를 쓰지 않는다.** Qt QSS에 box-shadow가 없기도 하고, 경계는 1px 선과
  여백으로 충분하다.

### 상세 스캔의 부하 — 예측 대신 측정

"얼마나 부하가 갈까"를 미리 답하지 않기로 했다. 소요 시간과 부하는 디렉터리
수·파일 수·파일시스템·캐시 상태에 따라 수십 배 차이가 나서, 돌려 보기 전에
내놓는 숫자는 근거가 없다. 1부 2절의 "UI 위기감 과장 금지"를 뒤집으면 "안심도
과장하지 않는다"가 되므로, 예측치를 지어내는 대신 **실제 실행을 기록**한다.

- `loadstat.py`가 체크포인트 하나가 끝날 때마다 CPU 점유를 표본으로 모으고,
  실행이 끝나면 평균·최대를 `scan_runs`에 남긴다. 다음 실행 전에 "지난번엔 이
  정도였다"로 판단할 수 있다.
- `top`을 실행해 파싱하지 않는다. 사람이 읽으라고 만든 출력이라 로케일·버전에
  따라 열이 달라져 cron(LANG=C)에서 깨지기 쉽다. `top`이 읽는 원본인 `/proc`을
  직접 본다 - 외부 명령도 하나 덜 띄운다.
- `/proc/self/stat`의 `cutime/cstime`에 **거둬들인 자식**의 CPU가 쌓인다.
  `du`/`find`를 `subprocess.run`으로 기다렸다 거두므로, 스캔 프로세스 하나만
  봐도 실제 작업량이 잡힌다.
- 퍼센트를 두 눈금으로 낸다: `top` 기준(코어 1개=100%)과 장비 전체 대비.
  사용자가 `top`을 띄워 놓고 대조할 때 숫자가 맞아야 하고, 동시에 "이 서버의
  몇 %"라는 질문에도 답해야 하기 때문이다.
- **메모리는 평균이 아니라 최고치를 남긴다.** "스캔이 메모리를 위협했나"에
  답하는 것은 순간 최대이고, `du`/`find`는 끝나면 사라져 사후 관측이 안 된다.
  커널이 기억해 주는 `getrusage(RUSAGE_CHILDREN).ru_maxrss`를 쓴다.
  시스템 여유는 `MemFree`가 아니라 `MemAvailable` 기준으로 본다 - 리눅스는
  남는 메모리를 캐시로 채우므로 MemFree를 부족으로 읽으면 매번 거짓 경보다.
- **한계를 같이 말한다.** 여기서 재는 것은 이 클라이언트의 CPU뿐이고, 상세
  스캔의 진짜 부담인 파일서버 I/O는 관측할 수 없다 (1부 2절 "부하에 대한
  겸손함"). 그래서 이 숫자를 "스토리지에 준 부하"라고 부르지 않는다.

진행 표시는 처음엔 불확정 막대만 뒀다가 나중에 바꿨다 - 아래 "진행률에 분모를
쓰기로 한 이유" 참고.

### 스냅샷 디렉터리 제외 (1부 7절 결정의 구현)

`.snapshot`/`.zfs`/`.ckpt`는 크기 측정과 변경 파일 집계 양쪽에서 뺀다. 세면 같은
데이터를 스냅샷 세대만큼 중복으로 세게 되어 계정 크기가 몇 배로 부풀고, 대개
읽기 전용이라 사용자가 정리할 수 있는 대상도 아니다.

- **큐에서 빼는 것만으로는 부족하다.** `du`는 주어진 디렉터리 아래를 스스로
  전부 걸어 내려가므로 체크포인트 큐와 무관하게 `.snapshot` 안까지 센다.
  제외는 반드시 명령 자체에 걸어야 한다 (`du --exclude=`, `find ... -prune`).
- `-prune`은 "내려가지 않는다"는 뜻이라 읽고 버리는 것이 아니라 처음부터 읽지
  않는다 - 결과뿐 아니라 부하도 준다.
- 1부 7절은 "계정 루트 바로 아래에서만 제외"로 적었지만, 구현은 **모든 깊이**
  에서 이름으로 제외한다. NetApp처럼 디렉터리마다 `.snapshot`이 붙는
  파일시스템에서는 루트만 빼는 것이 의미가 없기 때문이다. 사용자 데이터에 같은
  이름의 진짜 디렉터리가 있는 환경이라면 `detail_scan.SNAPSHOT_DIR_NAMES`에서
  빼면 된다.

### 용어: '세대'도 '몇 번째'도 아닌 **날짜**

내부 데이터 모델에서는 `generation`(세대)이 맞는 이름이다 - 스캔 한 바퀴를
세는 정수이고, 비교는 항상 "완료된 세대 vs 직전 완료 세대"로 이뤄진다.

그런데 **화면과 보고서에서는 이 단어가 사용자에게 아무것도 설명하지 않는다.**
사용자가 알고 싶은 것은 "직전 스캔과 비교하면 어떤가"이지 자료구조 이름이
아니다. "세대 3 기준"보다 "3번째 스캔 기준"이 같은 것을 더 정확히 말한다 -
세대라는 말은 시간 간격을 함축하지 않지만, 스캔은 실행 시점을 가리키기 때문이다.

처음에는 "3번째 스캔"으로 바꿨는데, 그것도 **회차 번호일 뿐 기준점이 아니다.**
사용자가 실제로 기억하는 단위는 날짜다 - `260819 스캔`은 그날 무슨 일이 있었는지와
바로 연결되지만 `3번째 스캔`은 아무것도 떠올리게 하지 않는다.

- `이전 세대 대비` -> `260819 대비` (열 제목이 비교 대상을 스스로 설명한다)
- `세대 3 기준, 직전 세대와 비교` -> `260820 스캔 기준, 260819 스캔과 비교`
- 보고서: `- project_a (260820 스캔)` + `기준: 260819 스캔 데이터 대비`
- 날짜는 `baseline_results.completed_at`에서 가져온다. 이미 세대별로 저장돼
  있어서 새 열이 필요 없었다.
- **아직 완료되지 않은 스캔은 날짜가 없다.** 그때는 `3번째 스캔`으로 물러난다 -
  진행 중인 것을 날짜로 부를 수는 없기 때문이다.
- 한국어는 사내 관례대로 `YYMMDD`, 영어는 오해가 없도록 `YYYY-MM-DD`를 쓴다.

**표시 문구만 바꿨다.** DB 열 이름(`generation`)과 함수 이름은 그대로 둔다 -
데이터 모델에서는 여전히 정확한 이름이고, 스키마를 바꾸면 기존 데이터
디렉터리를 옮기는 비용만 생기고 얻는 것이 없다.

날짜 포맷 헬퍼가 `widgets.scan_label`과 `reports._scan_label` 두 곳에 있다.
보고서 생성은 cron 경로에서 Qt 없이 돌아야 해서 GUI 모듈을 import할 수 없기
때문이다 - 규칙은 같고 의존성만 다르다.

### 보고서에 스캔 진행 상황을 넣은 자리

`du` 진행 개수와 **마지막으로 처리한 경로**를 **일간** 보고서에 넣었다.

주간이 아니라 일간인 이유: 아침에 보고서를 여는 목적이 대개 "밤새 얼마나
갔나"인데, 주간에만 있으면 그것을 주 1회만 볼 수 있다. 주간 보고서는 일간을
통째로 포함하므로 한 번만 넣으면 양쪽에서 다 보인다.

개수와 경로를 **둘 다** 적는 이유: 개수는 얼마나 남았는지만 말하고, 경로는
어느 대목에서 멈췄는지를 말한다. 밤새 돌다 끊긴 스캔을 아침에 볼 때 필요한
것은 후자다 - 경로를 보면 다음 밤에 어디부터 이어질지 짐작할 수 있다.

스캔 DB를 못 열어도 일간 보고서 자체는 나와야 하므로, 이 절은 실패하면 조용히
빠진다. 진행 상황은 부가 정보이고 등급·사용률 표가 본체다.

### 보고서 생성을 수집 경로로 옮긴 이유

일간 보고서가 매일 안 생긴다는 보고를 받고 보니, 보고서를 만드는 곳이
`run_nightly_scan` **하나뿐**이었다. 그런데 이 함수는 두 지점에서 일찍
돌아간다 - 시간창 밖일 때와 잠금이 이미 잡혀 있을 때. 그러면 보고서 생성
코드에 아예 닿지 않는다.

일간 보고서 내용은 df 표본 요약이라 **상세 스캔과 아무 상관이 없다.** 스캔이
안 돌았다고 함께 사라질 이유가 없었다. 15분 수집은 cron이 확실히 돌리므로
그쪽으로 옮겼다.

- `reports.due_kinds()`가 "지금 만들어야 할 것"을 판단하고, 이미 만든 것은
  다시 만들지 않는다. 15분마다 불려도 대부분은 파일 존재 확인 몇 번으로 끝난다.
- 주간은 "오늘이 그 요일인가"가 아니라 **"가장 최근 그 요일 이후로 만든 적이
  있는가"**로 판단한다. 당일에만 만들면 그날 장비가 꺼져 있었을 때 그 주
  보고서가 영영 생기지 않는다 - 밀렸으면 따라잡아야 한다.

### 99%를 넘으면 cooldown을 무시한다

cooldown은 "같은 이야기를 반복하지 않는다"는 규칙이고 대부분의 구간에서 옳다.
그런데 15분 표본이 이미 99%를 넘었다면 한 시간 뒤는 늦다 - 그 사이에 꽉 차서
쓰기가 실패한다. **이 구간에서는 조용한 것이 더 위험하다.**

기준을 등급(긴급 98%)과 따로 둔 이유: 등급은 '어느 색으로 보여줄까'의 기준이고
이 값은 '조용히 있어도 되는가'의 기준이라 성격이 다르다. 98%에서 한 시간에 한
번은 적절하지만 99%는 그렇지 않다.

트레이는 이 알림을 다른 것과 묶지 않고 따로, 마지막에 띄운다. 풍선이 한 번에
하나만 보이므로 나중에 띄운 것이 앞의 것을 덮는다 - 결과적으로 가장 급한 것이
화면에 남는다.

### 표 정렬 - 두 표에 서로 다른 방식을 쓴 이유

- **증가 경로 표**: Qt 내장 정렬(`setSortingEnabled`). 칸 위젯이 없어 안전하다.
- **계정 표**: 직접 정렬해 다시 그린다. 사용률 막대와 등급 배지가 칸 위젯인데,
  Qt 내장 정렬은 항목만 옮기고 위젯은 제자리에 두어 **행과 위젯이 어긋난다.**

정렬 키는 보이는 글자가 아니라 숫자를 쓴다(`widgets.NumericItem`). 글자로
정렬하면 `900.0 MB`가 `1.5 GB`보다 크게 잡혀서, 크기를 보려고 정렬했는데
결과가 거짓이 된다. 값이 없는 행은 방향과 무관하게 항상 뒤로 보낸다 - 빈 칸이
맨 위를 차지하면 정작 보려던 것이 밀린다.

계정 표를 정렬하면 행 번호가 설정 순서와 달라지므로, 행에서 계정을 찾을 때는
행 번호가 아니라 항목에 심어 둔 `account_id`를 쓴다.

### 진행률에 분모를 쓰기로 한 이유

처음에는 퍼센트를 일부러 안 보여줬다 - 디렉터리가 쪼개지면 총계가 늘어 진행률이
뒤로 갈 수 있기 때문이다. 그런데 "얼마나 남았는지 직관적으로 안 보인다"는
지적이 맞았다. 아무것도 안 보여 주는 것은 정직한 게 아니라 그냥 불친절하다.

지금은 `디렉터리 128/1,204 완료 (10%) · 분할되면 총계가 늘 수 있습니다`처럼
**개수와 한계를 함께** 적는다. 숫자를 지어내는 것이 아니라 실제 개수를 보여
주고 그 한계를 명시하는 것이라, "과장하지 않는 UI" 원칙과 충돌하지 않는다.

### 스캔 진행 위치를 보여주는 방법

`du` 하나가 몇 분씩 걸릴 수 있어서 "실행 중"만 떠 있으면 멈춘 것인지 진행 중인지
구분되지 않는다. 지금 훑고 있는 경로가 보이면 그 자체가 진행의 증거가 된다.

**체크포인트에 `running` 상태를 새로 만들지 않았다.** 재개 가능성이 "pending만
보면 된다"는 단순한 불변식(1부 7절)에 기대고 있는데, 중간 상태를 넣으면
프로세스가 죽었을 때 그 행을 pending으로 되돌리는 복구 로직이 따로 필요해지고,
그 복구가 빠지면 스캔이 영영 그 디렉터리를 건너뛴다. 대신 **실행 행(scan_runs)에
현재 경로를 적는다** - 표시 전용이라 죽어도 잃는 것이 없고, 실행이 끝나면 비운다
(남아 있으면 "아직 그걸 하고 있다"로 오해된다).

보여 주는 자리는 셋이다.

- **상세 스캔 탭**: `지금: <계정> · <종류> · <경로>` 한 줄.
- **홈 탭 배너**: 탭을 나눈 뒤 상세 스캔 탭을 열지 않으면 밤새 무엇이 도는지
  알 수 없어졌다. 움직이는 막대 + 현재 경로 + 그 탭으로 가는 버튼까지만 둔다.
- **진행 상황 다이얼로그**: 계정 하나의 체크포인트를 경로 단위로 나열한다.
  처리한 것을 위로, 대기 중인 것을 아래로 정렬한다 - 알고 싶은 것은 "어디까지
  했나"이고 대기 목록은 그 다음이기 때문이다. 읽기 전용이라 아무것도 실행하지
  않는다.

### 계정 표에서 읽히게 만든 것들

- **사용률 옆에 실제 크기를 둔다.** `95%`만으로는 남은 것이 5GB인지 5TB인지
  알 수 없어 급한 정도를 판단할 수 없다. 두 숫자는 **같은 단위로 맞춘다** -
  각자 단위를 고르면 `950.0 GB / 1.0 TB`처럼 나와서, 비교하라고 붙여 놓은
  표시가 오히려 암산을 요구한다.
  단위는 **TB에서 멈춘다.** 실제 계정은 많아야 수십 TB인데 PB로 올라가면
  `1.5 / 1.5 PB`처럼 뭉개져 남은 양의 차이가 사라진다. 100TB대까지 TB로 두면
  `99.1 / 100.0 TB`로 읽힌다. 작은 볼륨까지 TB로 끌어올리지는 않는다.
- **`파일시스템` 열을 뺐다.** 계정 대부분이 같은 파일시스템에 있어 같은 값이
  반복되며 열 하나를 차지했다. 확인하고 싶은 순간(이 계정이 어느 볼륨인가)에만
  보면 되는 값이라 경로 툴팁으로 옮겼다.
- **숫자 열은 오른쪽 정렬.** 여러 계정을 위에서 아래로 훑을 때 자릿수가 맞아야
  "어느 쪽이 큰가"가 읽지 않고도 보인다.
- **값 없음(`-`, `확인불가`, `예측 불가…`)은 흐리게.** inode를 지원하지 않는
  파일시스템에서는 그 열이 전부 `확인불가`가 되는데, 실제 값과 같은 색이면
  눈이 먼저 가야 할 곳이 흐려진다.
- 표의 숫자는 소수점 한 자리로 줄인다. 정확한 값은 툴팁에 남긴다 - 표는 훑는
  곳이고 툴팁은 확인하는 곳이라는 역할 분담이다.

### 화면 구조 - 단일 화면에서 탭 둘로 (2부 6절 결정 번복)

2부 6절은 "대시보드 단일 화면, 탭 없음"으로 결정했었다. 실제로 만들어 써 본
뒤 **뒤집는다.**

이유는 공간 배분이다. 한 화면에 쌓으니 상세 스캔 영역이 세로를 크게 차지해,
**매일 보는 현황(계정 표)이 가끔 보는 스캔 결과에 밀렸다.** 스플리터로 비율을
끌 수 있게 해 봤지만 그것은 "매번 사용자가 정리해야 한다"는 뜻이라 문제를 옮긴
것에 가까웠다.

- `홈` 탭: 히어로 + 동작 버튼 + 계정 표. 계정 표가 세로를 전부 가져간다.
- `상세 스캔` 탭: 시간창·진행 막대·증가 경로.

원래 결정의 취지(탭을 늘려 정보를 흩지 않는다)는 살린다 - 보고서·검색·계정
관리는 여전히 다이얼로그이고 탭으로 만들지 않았다. 탭은 **주기가 다른 두 가지**
(상시 확인 vs 밤에 한 번)를 가른 것뿐이다.

탭을 나누면서 생긴 문제 하나를 같이 해결했다: 증가 경로를 볼 계정을 홈 표에서
고르게 두면 상세 스캔 탭만 열어 놓고는 계정을 바꿀 수 없다. 스캔 탭 안에 계정
콤보를 두고, 홈 표 선택과 서로 따라가게 했다.

### 창을 닫을 때 자식 프로세스 정리

창을 닫아도 `du`가 계속 돌던 문제를 고쳤다. 예전 `closeEvent`는 중지 '요청'만
써 두고 바로 닫았는데:

- 요청은 체크포인트 **사이**에서만 확인되는데 시간의 대부분은 `du`가 도는 중이다.
- 창이 닫히면 파이썬이 종료되고 daemon 스레드는 잘리지만, **자식 프로세스는
  죽지 않고 init에 재부모화되어 끝까지 돈다** (앱과 같은 구조로 재현해 확인).

사용자에게는 "껐는데도 파일서버가 계속 느린" 상태로 나타난다. 1부 2-4절의
"강제 kill 없음"은 *임의의 PID에 신호를 보내지 않는다*는 뜻이고 같은 문장이
"실행 중인 하위 프로세스만 정리한다"고 명시하므로, 우리 자식을 정리하는 것은
원래 설계 안에 있었는데 구현이 빠져 있었다.

- 자식은 `/proc/<pid>/status`의 `PPid`가 내 pid인 것만 고른다. Popen 핸들을
  모아 두는 방식도 검토했지만, 이 저장소의 테스트가 `subprocess.run` 모킹을
  규약으로 쓰고 있어 호출 방식을 바꾸면 무관한 테스트 수십 개가 함께 깨진다.
  커널이 이미 부모-자식 관계를 알고 있으니 그것을 읽는 편이 낫다.
- SIGTERM -> (안 끝나면) SIGKILL 순서로 정리한다.
- 실행 행을 `stopped`로 마감하고 잠금도 푼다. 안 그러면 `running`으로 영영
  남아 다음에 열었을 때 "지금 뭔가 돌고 있다"로 오해하게 된다.
- 스캔 중에 닫으면 한 번 확인한다. 잃는 것은 진행 중이던 디렉터리 하나뿐이고
  그 체크포인트는 pending으로 남아 다음 스캔이 이어받는다.

### 계정 관리 화면 - 표 + 접힌 상세 설정

운영 형태가 "파트별 담당자 1~2명, 전체 10~20명"이라는 것이 이 화면의 설계를
정한다. 모든 사원이 쓰는 화면이 아니라 **소수가 서로의 계정을 넘겨다보는**
화면이다.

- 한 줄 목록(`이름 - 경로`)을 표로 바꾸고 **추가한 사람 / 추가일 / 최근 스캔일**을
  넣었다. 남이 등록한 계정을 볼 때 "이거 누가 넣었지"를 사람에게 물어보지
  않아도 되게 하는 것이 목적이다.
- `created_by`는 등록 시점의 OS 계정명이다. **인증이 아니라 메모다** - OS
  사용자명은 위조할 수 있고 이 앱은 검증하지 않는다. 그래서 권한 판단에는
  일절 쓰지 않는다.
- **예전 설정을 읽을 때 이 값을 채우지 않는다.** dataclass 기본값을 그대로 두면
  `created_by`가 없는 기존 config.json을 열 때 *지금 파일을 연 사람*이 등록자로
  둔갑한다. 모르는 값은 비워 두고 화면에 `-`로 보여 준다 (숫자를 지어내지
  않는다는 원칙의 사람 이름 판).
- **상세 설정(구 "전역 설정")은 기본으로 접는다.** 매일 하는 일은 계정 등록·확인
  이고, 알림 채널이나 quota 명령은 최초 한 번 정하면 몇 달을 안 건드린다. 늘
  펼쳐 두면 자주 쓰는 목록이 아래로 밀리고, 처음 여는 사람은 "이걸 다 정해야
  하나" 싶어진다.

### 구현 범위와 아직 안 만든 것

**phase 1** (2부 7절): 계정 등록 + df 기반 대시보드, 사용률
등급 표시(색상+텍스트), 15분 경량 수집(cron/타이머), 파일 outbox 알림, 최소
진단, 대시보드 단일 화면 + 한눈 요약.

**phase 2** (2부 8절 1번): 야간 상세 스캔. 1부 3·7절의
검증된 원칙을 계승하되 코드는 새로 썼다.

- 22:00~06:00 시간창 안에서만 cron 실행 (`scan_window.py`). 06:00이 되면 강제
  종료가 아니라 완료된 체크포인트를 남기고 `paused` 상태로 스스로 멈춘다.
- 터미널 직접 실행(`nightly_scan_cli.py --now`)은 의도적 진단/복구 경로라
  시간창 제한을 받지 않는다.
- 디렉터리 단위 체크포인트로 완전히 재개 가능 (`scan_store.py`) - 중단된
  다음 실행은 이미 끝낸 디렉터리를 다시 훑지 않는다.
- 강제 kill 없음: run ID 매칭 중지 요청 파일만 사용 (`scan_lock.py`).
  `nightly_scan_cli.py --stop` 또는 GUI의 `안전 중지` 버튼.
- 디렉터리 단위 타임아웃 초과 시 하위 디렉터리 작업으로 분할해 재시도.
- 기준선 비교는 "완료된 세대 vs 직전 완료 세대"를 **같은 경로끼리** 대조한다
  (기존 REVIEW.md가 지적한 "어제 top N vs 오늘 top N" 비교 오류를 피함).
- `nice`/`ionice`는 있으면 쓰고 없으면 그냥 진행 (최선 노력이지 처리량 상한이
  아님).
- 대시보드 아래쪽에 상세 스캔 영역을 추가 (탭 신설 없이 단일 화면 유지) -
  시간창/실행 상태/남은 작업 수와 선택 계정의 증가 경로를 보여준다. 전체
  분모를 모르는 진행률은 퍼센트로 부풀리지 않고 남은 작업 수로만 표시한다.

**phase 3** (나머지 기능 일괄 구현):

- **다국어(KOR/ENG)**: `i18n.py` 단순 dict 카탈로그. 상단 `언어` 메뉴에서 즉시
  전환되며 재시작이 필요 없다. 저장/전송되는 값에는 언어 중립 코드(`tier` 등)를
  항상 함께 남겨 나중에 다른 언어로 다시 렌더링할 수 있다.
- **quota 어댑터**: shell 없이 실행하는 argv 배열로 `{account}`/`{path}`를
  치환해 JSON을 받는다. quota 조회가 실패해도 `df` 수집은 그대로 살린다.
  한도가 0/없음이면 사용률을 계산하지 않는다(0으로 나눠 100%처럼 보이지 않게).
- **알림 채널**: 기본 파일 outbox에 더해 사내 command(stdin UTF-8 JSON, shell
  미사용)와 내부 webhook(urllib POST). 어떤 모드든 원문과 전송 결과를
  `notify_audit/`에 남긴다. 설정이 덜 됐으면 outbox로 안전하게 떨어진다.
- **보고서**: 일간/주간/정리 후보. 정리 후보는 "충분히 크고 + 2세대 이상
  관찰했고 + 크기가 변하지 않은" 경로만 올리며, **어떤 파일도 삭제하지
  않는다**. 언어별로 저장한다.
- **트레이 notifier**: `notifier_cli.py`가 메인 GUI와 독립 실행된다. 팝업을
  띄운 것만으로 읽음 처리하지 않고 사용자가 확인했을 때만 처리하며, 로그아웃
  중 쌓인 알림은 다음 실행에서 한 번에 요약한다. XDG autostart 등록/해제 지원.
- **검색 인덱스**: 파일 **이름만**(내용 아님) 별도 `search_index.db`에 저장.
  계정별 opt-in, 기본 꺼짐. 루트 `.snapshot` 제외·심볼릭 링크 미추적·파일시스템
  경계 유지. 관리자 PIN은 **화면 노출 제한**이지 보안 경계가 아님을 코드와
  화면 양쪽에 명시했다.
- **최초 실행 안내**: 계정이 없을 때 진단 결과와 다음 할 일을 한 화면에
  보여준다.

**phase 4** (마무리):

- **급증 알림**: 야간 스캔이 기준선을 완주하면 직전 세대와 **같은 경로끼리**
  비교해 `growth_alert_min_kb`(기본 100GB)를 넘는 증가를 알린다. 알림 종류를
  `kind`로 구분(`capacity`/`growth`)하고 cooldown 키 공간도 분리해, 용량
  알림이 정상 복귀로 리셋될 때 급증 기록이 지워지지 않는다. 비교할 이전
  세대가 없는 첫 기준선에서는 알리지 않는다 - 전부 '신규'로 잡혀 의미 없는
  알림 폭탄이 되기 때문. 임계치는 통계 추정이 아니라 설명 가능한 단순 절대
  값 하나만 쓴다.
- **검색 인덱스 자동 갱신**: 검색을 켠 계정은 야간 스캔에서 인덱스를 갱신
  한다. 시간창/안전 중지 규칙을 `du`/`find`와 똑같이 따르고, 인덱싱 실패가
  이미 저장된 기준선을 무효로 만들지 않는다. 설정에서 사라진 계정의 orphan
  인덱스도 매 실행마다 정리한다.
- **관리자 PIN 변경**: 검색 화면에서 변경할 수 있고, 기본 PIN을 쓰는 동안은
  경고를 표시한다. 설정에는 PBKDF2 해시만 저장한다 (평문 아님). 다만 이는
  여전히 화면 노출 제한이지 보안 경계가 아니다 - 검색 DB는 암호화되지 않으며
  파일을 직접 읽을 수 있는 사람은 PIN과 무관하게 내용을 볼 수 있다.

**phase 5** (FULL 도달 예측):

15분 표본을 **최소제곱 회귀**로 분석해 "이 파일시스템이 언제 차는가"를
추정한다. 첫/끝 두 점 차이가 아니라 창 안의 모든 점을 쓰는 이유는, `df` 값이
파일 생성·삭제로 톱니처럼 흔들려 두 점만 보면 우연히 잡힌 순간값에 예측이
통째로 휘둘리기 때문이다.

| 용도 | 창 | 기대 표본 | 최소 표본 |
|---|---|---|---|
| 임박 판정 | 최근 3시간 | 12 | 4 |
| 단기 추세 | 최근 7일 | 672 | 24 |
| 장기 추세 | 최근 30일 | 2,880 | 96 |

최소 표본을 기대치보다 훨씬 낮게 잡은 것은 cron 누락이나 재시작으로 표본이
빠져도 예측이 통째로 멈추지 않게 하기 위함이다.

- **예측 불가 규칙**: 표본 부족 / 기울기 ≤ 0(감소·정체) / 도달 예상 10년 초과.
  숫자를 만들지 않고 사유와 함께 `예측 불가`로 표시한다.
- **경고 등급**: 2시간 내 → 긴급, 6시간 내 → 경고, 3시간 내 100GB 이상 증가
  → 주의. 급증 판정은 회귀가 아니라 실제 관측값 차이를 쓴다 ("얼마나 늘었나"는
  추정이 아니라 사실이어야 한다).
- **공유 파일시스템 중복 제거**: `df`는 파일시스템 전체 값이라 같은 fs의
  계정들은 예측이 같다. 알림은 fs당 1건으로 합치고 관련 계정 목록을 메시지와
  `details.accounts`에 담는다. 대시보드는 계정별로 표시한다.
- **대시보드 `FULL 예상` 컬럼**: 임박(48시간 이내)이면 시간 하나만, 평상시에는
  `약 12일 / 약 45일`(7일·30일 기준)을 나란히 보여 추세가 빨라졌는지 판단하게
  한다. 툴팁에 기울기와 "추정치" 단서를 명시한다.

**경로별 증감 이력 축적**: 이상탐지는 아직 구현하지 않았지만 나중에 붙일 수
있도록 `growth_history(경로, 세대, 증감KB)` 숫자만 축적한다. `baseline_results`
는 DB 크기 때문에 2세대만 남기지만, 이 테이블은 행이 작아 **60세대**까지
보관한다 - 이력이 짧으면 이상탐지 자체가 불가능하기 때문.

**아직 만들지 않은 것**:

- 급증 판정에 중앙값·MAD 같은 이상탐지를 쓰는 것. 이제 이력은 쌓이기
  시작하므로, 세대가 충분히 모이면 `growth_history_for_path()`를 입력으로
  붙일 수 있다. 그 전에 구현해 봐야 계산할 데이터가 없다.

### 알려진 제약 / 로컬 개발 환경 메모

- 단위 테스트는 Windows + Python 3.10에서도 전부 통과한다 (Qt 없이도
  동작하도록 GUI 의존을 분리해 뒀기 때문). Git Bash가 있는 Windows에서는
  `df`/`du`/`find`도 있어서 GUI 실행, 15분 수집, 상세 스캔(`--now`)까지
  실제로 돌려 확인했다. 다만 `df -Pi`가 NTFS에서 inode를 보고하지 않아
  inode 등급은 `확인불가`로 표시된다 - 숫자를 지어내지 않는 정상 동작이다.
- 실제 VWP(RHEL, Python 3.12)에서 재확인이 필요한 것: Python 3.12 + PyQt5
  조합과 xcb 플러그인 로드, `crontab` 등록 권한, `nice`/`ionice` 존재 여부,
  대용량 계정에서의 실제 스캔 소요 시간과 06:00 인계 동작.
- `run.csh` / `setup_cron.csh`는 csh 스크립트라 Windows에서 직접 실행/검증할
  수 없었다 - 로직은 기존 루트 `run.csh`의 검증된 패턴(진단 실패 시 exit 2,
  `PYTHONHOME` 경고 후 제거 등)을 그대로 계승했다.
