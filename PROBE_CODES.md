# 진단 코드표 (PROBE v5)

`./smvwp_cli.py probe --path <경로>` 가 내놓는 코드의 뜻.

```
== SMVWP PROBE v5 ==
CODE A1B2C1D2 E3F1G1H3 I3J1K1L2 M3N2O2P1 Q2R2S2T1 U2V1W1X1 Y1Z1 #7c
CPU  A3B2C1D2 E3F3G2H3 #c5
VAL  H=87.8/36.4 I=1.00/1.62/1.95/1.98 J=0.000% K=0.52 cC=0.18 cD=0.61 ...
```

이 줄들만 옮겨 적으면 된다. 아래 표가 그 뜻이다.

**`CODE` 와 `CPU` 는 서로 다른 표다.** 같은 `A` 라도 뜻이 다르므로 **줄 이름을
반드시 함께** 적어야 한다. `VAL` 에서 `CPU` 쪽 값은 앞에 `c` 가 붙는다
(`cD=0.61`).

**`#7c` 는 검사합이다.** 줄을 옮겨 적다 한 글자라도 틀리면 값이 달라지므로,
받는 쪽에서 다시 계산해 맞춰 보면 오타를 잡을 수 있다. 두 줄은 검사합도
따로다.

**숫자 `0` 은 언제나 "재지 못함"이다** — 없거나, 불가능하거나, 건너뛴 것.
"쟀는데 가장 낮은 값"(=1)과 헷갈리면 안 된다.

버전(`v5`)이 다르면 이 표로 읽으면 안 된다. 뜻이 바뀔 때마다
`probe.CODE_VERSION` 을 올린다.

---

## 환경 (A~G) — 실행 비용 없음, 파일 몇 개를 읽는 게 전부

| 코드 | 항목 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| **A** | 대상 파일시스템 | nfs4 | nfs3 | 로컬(xfs/ext4 등) | 기타 |
| **B** | NFS 버전 | v4.1 이상 | v4.0 | v3 | NFS 아님 |
| **C** | nconnect | 없음(연결 1개) | 2~3 | 4 이상 | NFS 아님 |
| **D** | rsize | 64K 이하 | 128K | 256K | 그 이상/불명 |
| **E** | RPC 슬롯 상한 | 16 이하 | 17~64 | 65~128 | 129 이상 |
| **F** | 속성 캐시 | 기본값 | **noac/actimeo=0** | actimeo 지정 | NFS 아님 |
| **G** | readdirplus | 사용 | **nordirplus** | — | NFS 아님 |

읽는 법:

- **C1** 이면 커널이 서버당 TCP 연결을 하나만 쓴다. 스레드를 아무리 늘려도
  한 연결에 줄을 서므로, `I` 가 낮게 나오는 가장 흔한 원인이다.
- **F2** 면 `stat` 마다 서버로 왕복한다. 순회가 느린 이유가 여기 있을 수 있다.
- **G2** 면 디렉터리를 읽은 뒤 파일마다 `stat` 을 또 보낸다. 메타데이터 왕복이
  몇 배로 늘어난다.
- **E1** 이면 슬롯이 16개뿐이라 동시 요청의 천장이 낮다.

## 속도 (H, J, N, O, P)

| 코드 | 항목 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| **H** | 순회 대 `du` 배속 | 1.2배 미만 | 1.2~2배 | 2~4배 | 4배 이상 | **순회가 더 느림** |
| **J** | 크기 일치 | 완전 일치 | 0.1% 미만 차이 | 1% 미만 | **1% 이상 차이** | du 값 없음 |
| **N** | 단일 스레드 지연 | 0.02ms 미만 | 0.02~0.05ms | 0.05~0.2ms | **0.2ms 이상** |
| **O** | 최대 처리량 | 5만/초 이상 | 1만~5만 | 2천~1만 | **2천 미만** |
| **P** | 캐시 영향 | 10% 미만 | 10~30% | **30% 이상** |

- **H** 는 `du` → 순회 → `du` 순으로 재서 **앞뒤 du 의 평균**과 비교한다.
  한쪽만 재면 나중에 돈 쪽이 캐시 덕을 보고, 그 상태에서 나온 승패는 아무것도
  말해 주지 않는다. `VAL` 의 `H=87.8+118.0/36.4` 는 `앞du+뒤du/순회` 다.
  - **v2 이하의 H 는 믿을 수 없다.** 순회를 먼저, `du` 를 나중에 한 번만
    돌려서 `du` 에게 캐시가 유리했다. 특히 `H5`(순회가 더 느림)가 나왔다면
    그것이 순회 탓인지 `du` 가 유리했던 탓인지 가릴 수 없다.
  - 앞뒤 `du` 가 크게 다르면 그 자체가 정보다 - 이 장비에서는 캐시나 부하
    변동이 측정을 지배한다는 뜻이라, 한 번의 비교로 결론을 내면 안 된다.
- **J4** 면 아래의 속도 이야기는 전부 무의미하다. 값이 다르면 빠른 게 의미가
  없다. (다만 **Z2** 인 환경에서는 근사값이라 J 가 커도 정상이다.)
  - 두 번 다 **소수점까지 같은** 0.543% 가 나온 것이 실제로 버그를 잡아냈다.
    처음에는 비교용 `du` 에 `--exclude` 가 없어서라고 봤는데(v5 에서 고침),
    값이 그대로였다. 진짜 원인은 **순회가 디렉터리 자신의 블록을 안 세던
    것**이었다 - `du` 는 센다. 디렉터리 6만 개면 그만큼이 통째로 빠진다.
    (`walker._scan_one` 에서 고쳤다.)
  - 규칙적으로 재현되는 작은 차이는 경합이 아니라 **규칙의 차이**다. 값이
    흔들리지 않으면 그쪽을 먼저 의심할 것.
- **N** 은 `1 / 단일 스레드 처리량`이다. NFS 왕복 지연의 대리 지표다.
- **P3** 이면 **I 를 믿으면 안 된다.** 뒤에 잰 것일수록 캐시가 더워서 빨랐다는
  뜻이라, 스레드를 늘려서 빨라진 것처럼 보였을 수 있다.

## 스레드 확장 (I)

| 코드 | 뜻 |
|---|---|
| **I1** | x1 위로는 안 늘어남 (연결/서버가 이미 천장) |
| **I2** | x2 까지만 늘어남 |
| **I3** | x4 까지 늘어남 |
| **I4** | x8 까지 늘어남 |
| **I5** | x16 까지 늘어남 |
| **I6** | x32 까지도 계속 늘어남 (더 올릴 여지가 있다) |
| **I0** | 재지 못함 |

`VAL` 의 `I=1.00/1.62/1.95/1.98/...` 은 x1 대비 배율이다
(x1/x2/x4/x8/x16/x32 순, `--quick` 이면 x1/x4/x16).

**스윕이 32까지 가는 이유**: 실기의 RPC 슬롯 상한이 128이고 순회는 32코어 중
0.19코어밖에 안 쓴다. 스레드 4개는 슬롯 128개 중 4개를 쓰는 것이라 천장 근처에도
못 간 상태다. 8에서 끊으면 "여기가 한계"인지 "여기까지만 쟀다"인지 구분되지 않는다.

## 동시성 (K, L, M) — 이 진단의 핵심

셋 다 **같은 방식**으로 잰다: 각자 4스레드로 혼자 돌 때와 동시에 돌 때의 속도
비율. 그래서 세 값을 나란히 놓고 읽을 수 있다.

| 코드 | 뜻 |
|---|---|
| **1** | 0.6 미만 — 서로 방해한다 (동시에 돌려도 합이 거의 안 늘어남) |
| **2** | 0.6~0.85 — 절반쯤 이득 |
| **3** | 0.85 이상 — 각자 제 속도를 지킨다 (동시 실행이 그대로 이득) |
| **0** | 짝이 없어 재지 못함 |

| 코드 | 무엇과 무엇 |
|---|---|
| **K** | 같은 볼륨의 두 하위 디렉터리 (파일시스템에서 직접 고른다. 루트
바로 아래에 둘이 없으면 한 단계씩 내려간다) |
| **L** | **같은 파일러의 다른 볼륨** ← 아직 답이 없던 질문 |
| **M** | 다른 파일러 |

판단표:

| K | L | M | 뜻과 조치 |
|---|---|---|---|
| 1 | 3 | 3 | 예상대로. 볼륨이 한계다. `nightly_parallel_accounts` 를 볼륨 수만큼 올려도 안전 |
| 1 | 1 | 3 | **파일러가 한계.** 파일러당 하나씩만 돌려야 한다 (지금 코드에 없는 제한) |
| 1 | 1 | 1 | 병목이 저장소가 아니라 **이 서버**다 (NIC/CPU/RPC 계층). 병렬을 늘려도 소용없음 |
| 3 | 3 | 3 | 아무 데도 포화가 아니다. 스레드부터 더 올려 볼 것 |

`VAL` 에 실제 비율이 소수로 들어간다 (`K=0.52`).

## CPU 와 프로세스 (`CPU` 줄) — "안 빨라진다"의 원인을 가른다

같은 "안 빨라짐"이라도 원인이 셋이다: 우리가 CPU를 다 태웠거나(→프로세스로
나누면 듣는다), 기다리기만 했거나(→서버/연결이 천장), 장비가 이미 바빴거나.
속도만 재면 이 셋이 구분되지 않는다.

| 코드 | 항목 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| **A** | 논리 CPU 수 | 8 미만 | 8~31 | 32~63 | 64 이상 |
| **B** | 하이퍼스레딩 | 없음 (1코어=1스레드) | 2배 (HT) | 2배 초과 |
| **C** | x1 CPU 점유 | 0.25코어 미만 (**기다리는 중**) | 0.25~0.7 | 0.7~1.0 (**한 코어 포화**) | 1.0 이상 |
| **D** | 병렬 CPU 점유 | 1코어 미만 | 1~2 | 2~4 | 4코어 이상 |
| **E** | iowait | 5% 미만 | 5~20% | 20~50% | **50% 이상** |
| **F** | 프로세스 확장 포화점 | x1 | x2 | x4 | x8 (5=x16, 6=x32) |
| **G** | 프로세스 대 스레드 | **프로세스가 1.2배 이상 빠름** | 비슷 (0.8~1.2) | 스레드가 더 빠름 |
| **H** | 최대 달성 배율 | 1.2배 미만 | 1.2~2배 | 2~4배 | 4배 이상 |

**읽는 순서는 C·D·E → G 다.**

| C·D | E | G | 결론 |
|---|---|---|---|
| 낮음 | 높음 | 2 | **저장소가 천장.** 기다리기만 했고 프로세스로 나눠도 같다. 병렬을 더 올려도 소용없다 |
| 낮음 | 낮음 | 2 | 어느 쪽도 안 바쁘다 → **연결이 천장** (`CODE C1` = nconnect 없음을 확인할 것) |
| 높음 | 낮음 | **1** | **GIL 이 병목.** 프로세스로 나누는 것이 답이다 |
| 높음 | 낮음 | 2 | 파이썬 자체가 느린 것 (순회 코드가 CPU를 태움) |

`C1 + E4` 조합이 지금까지의 예상이다 — 우리는 거의 아무 일도 안 하고 NFS 왕복만
기다린다는 뜻. 그렇다면 **스레드를 늘리는 것이 맞는 방향**이고, 그런데도 `I` 가
낮으면 원인은 우리 밖(연결·서버)에 있다.

`F`·`G` 가 `0` 이면 프로세스 측정을 못 한 것이다 (`fork` 불가, 또는 나눌 하위
디렉터리가 2개 미만). 리눅스에서는 정상적으로 잰다.

## 트리 모양 (Q~V)

| 코드 | 항목 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| **Q** | 파일 수 | 10만 미만 | 10만~100만 | 100만~1000만 | 1000만 이상 |
| **R** | 디렉터리당 평균 파일 | 10 미만 | 10~100 | 100~1000 | 1000 이상 |
| **S** | 최대 깊이 | 5 이하 | 6~10 | 11~20 | 21 이상 |
| **T** | 하드링크 비중 | 없음 | 1% 미만 | 1~10% | 10% 이상 |
| **U** | 읽기 실패 비중 | 없음 | 0.1% 미만 | 0.1~5% | **5% 이상** |
| **V** | 점유/논리 크기 | 0.95~1.05 (보통) | 1.05 초과 (작은 파일 많음) | 0.95 미만 (희소 파일) |

- **R4** 면 디렉터리 하나에 파일이 수천 개다. `readdir` 한 번이 무거워지고,
  체크포인트를 쪼개도 그 디렉터리는 못 쪼갠다.
- **T3/T4** 면 하드링크가 많다. 체크포인트를 나눠 세면 **같은 파일을 두 번
  셀 수 있다** — `du` 도 같은 문제를 가진다.
- **U3/U4** 면 측정값이 실제보다 작다. 권한 문제를 먼저 풀어야 한다.
- **S4** 면 `detail_scan_max_depth` 가 트리를 다 못 담는다.

## 장비 (W~Z)

| 코드 | 항목 | 1 | 2 | 3 |
|---|---|---|---|---|
| **W** | 부하 여유 | load/cpu < 0.3 (한가) | 0.3~0.7 | **0.7 이상 (이미 바쁨)** |
| **X** | 순회 최대 메모리 | 200MB 미만 | 200MB~1GB | **1GB 이상** |
| **Y** | nice/ionice | 둘 다 있음 | nice 만 | 둘 다 없음 |
| **Z** | st_blocks | 지원 (정확) | **미지원 (근사값)** |

- **W3** 이면 위의 속도 숫자를 낮춰 읽어야 한다. 우리가 느린 게 아니라 장비가
  이미 바빴을 수 있다.
- **Z2** 는 개발 PC(윈도우)에서만 나온다. 그 경우 **J 가 커도 정상**이다.

## VAL 줄에 들어가는 원값

코드로는 못 담는 값만 짧게 붙는다.

| 키 | 뜻 |
|---|---|
| `A` | export 이름 (`ecfiler:/vol/cae`) |
| `H` | `앞du초+뒤du초/순회초` |
| `I` | x1 대비 배율 (스레드 수 순서대로) |
| `J` | 크기 차이 % |
| `K` `L` `M` | 동시 실행 시 속도 유지 비율 |
| `N` | 항목당 ms |
| `O` | 초당 항목 수 |
| `P` | 첫 x1 대비 마지막 x1 차이 % |
| `Q` `R` `S` | 파일 수 / 평균 팬아웃 / 최대 깊이 |
| `T` `U` | 하드링크 % / 읽기 실패 수 |
| `V` | 점유÷논리 |
| `W` `X` | `load/cpu` / 최대 메모리 |
| `cA` | 논리/물리 CPU 수 |
| `cC` `cD` | x1 / 병렬 구간에서 쓴 코어 수 |
| `cE` | iowait % |
| `cF` | 프로세스 x1 대비 배율 |
| `cG` | 프로세스 ÷ 스레드 |
| `cH` | x1 대비 최대 배율 |

---

## 실행 방법

```bash
./smvwp_cli.py probe --path /user/cae/과제A
```

- 아무것도 쓰지 않는다 (읽기 전용). 대상 경로도 스캔 DB 도 건드리지 않는다.
- 걸리는 시간 ≈ **전체 순회 1회 + `du -sk` 2회(앞뒤) + 조각 26개 × 15초**.
  전체는 대략 20~30분이다. `du` 가 느린 계정이면 그쪽이 더 걸린다
  (실기에서 찬 캐시 `du -sk` 하나가 478초였다).
- 짧게 보려면 `--quick --seconds 8` (조각이 절반으로 줄어 5~8분).
- 프로세스 측정은 프로세스마다 스레드 2개를 쓴다 (`--process-threads`). 그래야
  `proc x4` 가 총 8 동시로 스레드 x8 과 견줄 수 있다.
- 이웃 계정과 비교(L·M)는 등록된 계정에서 자동으로 짝을 찾는다. 직접 주려면
  `--peer <같은 파일러의 다른 볼륨>` `--peer2 <다른 파일러>`.
- 옮겨 적을 줄만 보려면 `--codes-only`.
