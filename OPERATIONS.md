# 운영 안내

이 도구가 **언제 무엇을 하는지**를 사용자 입장에서 적었다. 코드를 몰라도 읽을
수 있게 썼고, 인수인계할 때 이 문서만 넘겨도 되도록 만들었다.

설계 이유가 궁금하면 `DESIGN.md`, 진단 코드는 `PROBE_CODES.md`.

---

## 하루가 어떻게 도는가

```
00:00 ─────────────────────────────────────── 22:00 ──────── 06:00
  │                                              │              │
  └─ collect (15분마다, 하루 종일) ──────────────┴─ scan ───────┘
     df 한 번씩. 몇 초.                            상세 스캔
                                                   8시간 내내 한 프로세스
```

cron 에 등록되는 것은 **두 줄뿐**이고 그게 전부다.

```bash
crontab -l | grep storage_manager_vwp
```

두 줄이 안 나오면 `./setup_cron.csh` 를 한 번 돌린다.

---

## 1. `collect` — 15분마다, 하루 종일

```
*/15 * * * *   smvwp_cli.py collect
```

계정마다 `df` 를 한 번 부른다. "이 계정 총 10TB 중 8.2TB 씀" 이 숫자만
받아온다. 몇 초면 끝나고 파일서버에 부담이 거의 없다.

**왜 15분마다인가.** 이 숫자들이 쌓여야 증가 추세가 나오고, 그래야 "이 속도면
사흘 뒤 full" 같은 예측이 성립한다. 하루 한 번으로는 점이 너무 적다.

**여기서 알림이 나간다.**

- 사용률이 임계(기본 90% / 95%)를 넘으면 즉시
- 추세상 곧 full 이 예상되면 예측 알림

대시보드 홈 탭의 숫자가 이것이다. 15분마다 갱신된다.

---

## 2. `scan` — 22:00 에 뜨고 06:00 까지

```
0 22 * * *   nice -n 10 ionice -c2 -n7  smvwp_cli.py scan
```

**"상세 스캔"이 이것이다.** 별도의 "deep" 명령이 따로 있는 것이 아니다.
22시에 프로세스 하나가 뜨고 06시까지 죽지 않고 계속 돈다.

계정마다 두 가지를 한다.

| | 무엇을 | 왜 |
|---|---|---|
| 기준선 | 디렉터리별 용량 (`과제A/LAYOUT/... = 300GB`) | 어디가 큰지 |
| 검색 인덱스 | 켜 둔 계정만 | 파일 검색용 |

예전에는 `find` 로 최근 바뀐 파일 수를 세는 단계가 하나 더 있었다. 없앴다 -
트리를 한 번 더 완주하느라 밤 작업이 두 배였는데, 나온 숫자로 할 수 있는
일이 없었다. 무엇이 늘고 있는지는 세대별 증감과 큰 파일 목록이 더 구체적으로
답한다.

`df` 는 "계정 전체가 얼마"만 알려 준다. 이쪽은 **계정 안 어디가 얼마인지**
파고들기 때문에 오래 걸린다.

### 8시간에 다 못 돌면

**정상이다.** 06:00 이 되면 하던 자리에 책갈피(체크포인트)를 남기고 멈춘다.
다음 밤 22시에 거기서 이어받는다.

두 가지 성질이 중요하다.

- **버려지는 작업이 없다.** 중간까지 잰 것은 이미 저장돼 있다.
- **계정 순서가 매일 돌아간다.** 안 그러면 목록 뒤쪽 계정이 영원히 차례가
  오지 않는다.

계정이 많거나 크면 한 바퀴에 며칠 걸릴 수 있다. 진행 상황은 GUI 상세 스캔
탭에서 계정별로 "몇 개 남음 / 예상 얼마" 로 보인다.

### 계정 안이 궁금할 때

계정 표는 "LAYOUT 이 420GB" 까지만 말한다. 그 안이 궁금하면 상세 스캔 탭의
`폴더 펼쳐 보기` 를 누른다.

```
/proj                          900.0 GB  100.0%   +200.0 GB
   SIM                         300.0 GB   33.3%   +180.0 GB
      waves                    280.0 GB   31.1%   +180.0 GB
         파일: dump_2609.vcd   150.0 GB   16.7%   +110.0 GB
```

**다시 재지 않는다.** 야간 스캔이 이미 남긴 기록만 읽으므로 창을 연다고
파일서버에 부하가 가지 않는다.

회색 줄(`이 폴더에 직접 있는 파일`, `그 밖 폴더 N개`)은 계산으로 만든 줄이라
더 들어갈 수 없다. 크기가 자기 자신을 포함한 합계라, 하위 폴더를 다 더해도
부모보다 작으면 그 차이가 이 폴더에 직접 있는 파일이다.

### 얼마나 빠른가

파이썬 순회로 재는데, 실기 실측에서 `du -sk` 보다 **5.7배** 빨랐다
(찬 캐시 `du` 487초 대 순회 85.5초, 파일 83만 개짜리 계정). 크기는 `du` 와
정확히 일치한다.

되돌리려면 계정관리 설정의 `측정 방법` 을 `du 실행` 으로 바꾼다. 다른 설정은
건드릴 필요가 없다.

---

## 3. 스캔이 끝나면 — 보고서

`scan` 프로세스가 끝날 때(다 돌았든 06시에 멈췄든) 자동으로 만든다.

| 보고서 | 주기 |
|---|---|
| 일간 | 매일 |
| 정리 후보 | 매일 |
| 주간 | 설정한 요일에만 |

`<데이터디렉터리>/reports/daily/` 에 쌓인다. **아침에 출근해서 이것을 본다.**

날짜는 **끝난 아침** 기준이다. 목요일 22시에 시작해 금요일 06시에 끝났으면
금요일자다 - 읽는 사람이 금요일 아침에 열기 때문이다.

밤새 크게 늘어난 경로가 있으면 여기서도 알림이 나간다.

---

## GUI 는 cron 과 상관이 없다

창을 껐다 켜도 위의 두 cron 은 그대로 돈다. GUI 가 하는 일은 셋이다.

1. **보는 창** - 쌓인 데이터를 읽어서 보여 준다
2. **수동 실행** - `지금 스캔` 버튼 (22시를 안 기다리고 지금 시작)
3. **설정** - 계정 등록, 성격 지정, 임계값 등

창을 켜 두면 15분 수집이 창 안에서도 한 번 더 돈다. 겹쳐도 문제없다.

### 창만으로 밤을 지키고 싶다면

계정관리 설정의 `야간 자동 스캔` 을 켜면, 창이 켜져 있는 동안 22시에 스스로
시작하고 **창을 닫으면 함께 멈춘다.** 이 경우 cron 의 야간 줄은 지워야 한다.

```bash
./setup_cron.csh --collector-only
```

대가가 있다. **아무도 창을 켜 두지 않은 밤은 통째로 빈다.** 그날 밤 스캔이
없으면 다음 날 아침 감지가 하루 늦는다.

수집(15분)은 cron 에 남기는 편이 낫다. 창을 닫은 동안 이력이 끊기면 예측이
어긋난다.

---

## 업데이트할 때 - cron 을 잠시 내린다

파일을 갈아끼우는 도중 cron 이 뜨면 **반쯤 쓰인 코드를 읽는다.** 돌고 있는
프로세스 밑에서 `.py` 를 바꾸면 나중에 읽어들이는 모듈만 새것이 되어 옛 코드와
섞이기도 한다. 교체 전에 내려 두는 편이 안전하다.

```bash
./setup_cron.csh --remove          # 이 도구가 등록한 두 줄만 지운다
pgrep -af smvwp_cli.py             # 돌던 것이 남았는지
smvwp_cli.py scan --stop           # 스캔이 돌고 있으면 (낮이면 pkill 도 무방)
```

`crontab -l` 로 두 줄이 사라진 것을 확인한 뒤 파일을 교체한다. **다른 사람이
넣은 cron 항목은 건드리지 않는다** - 명령줄이 아니라 줄 끝의 표식으로 우리
것만 찾는다.

교체가 끝나면 실행 권한을 되살리고(ZIP 은 실행 비트를 잃는다) 다시 올린다.

```bash
chmod +x run.csh setup_cron.csh check_env.sh
./run.csh --diagnose               # 잘 붙었는지 확인
./setup_cron.csh                   # 다시 등록
crontab -l | grep storage_manager_vwp
```

`--remove` 는 환경변수가 없어도 돈다 - 지우는 데는 파이썬 경로도 데이터
디렉터리도 필요 없다.

**21:5x 에는 하지 않는다.** 압축을 푸는 중에 22시가 되면 그 밤을 잃는다.

---

## GUI 와 cron 이 겹칠 때 - 경우의 수

동시에 두 개가 도는 일은 없다. 잠금 파일 하나가 프로세스를 가로질러 막는다.
문제는 "누가 물러나느냐"인데, 경우마다 다르다.

| 상황 | 무슨 일이 | 결과 |
|---|---|---|
| cron 이 도는 중 `지금 스캔` 을 누름 | 버튼 쪽이 **즉시** 거절 | "이미 실행 중입니다 (cron)" |
| 낮에 누른 것이 도는 중 22:00 | 낮 실행이 **스스로 물러나고** cron 이 이어받음 | 밤을 잃지 않는다 |
| 밤에 누른 것이 도는 중 cron | cron 이 최대 20분 기다렸다 그래도 안 되면 물러남 | 어차피 같은 일을 하는 중 |
| 창을 닫음 | 창이 띄운 것만 멈춤 | cron 것은 계속 돈다 |
| GUI 자동 야간 + cron 야간 둘 다 켬 | 먼저 잡은 쪽이 돌고 나머지는 물러남 | 안전하지만 "닫으면 멈춤"은 깨진다 |

### 낮에 누른 스캔은 22:00 에 물러난다

이것이 가장 중요한 규칙이다. 낮에 `지금 스캔` 을 누르면 시간창을 무시하고
돌지만, **야간 창이 열리면 스스로 멈춘다.** 안 그러면 사람이 낮에 눌러 둔 것
하나가 그날 밤 전체를 먹는다.

물러나도 잃는 것은 없다. 재던 디렉터리를 마저 끝내고 책갈피를 남기므로, 곧
뜨는 야간 실행이 거기서 이어받는다.

그래서 cron 은 22:00 에 잠금이 안 잡히면 **최대 20분 기다린다.** 낮 실행이
물러나는 데 재던 디렉터리 하나만큼(최대 15분) 걸리기 때문이다.

**야간 창 안에서 누른 것은 물러나지 않는다.** 그것이 곧 그날 밤의 실행이라
양보할 상대가 없다.

### 낮에 눌러도 되는가

된다. 다만 사용자와 파일서버를 나눠 쓰게 되므로, 급하지 않으면 밤에 맡기는
편이 낫다. 부하가 궁금하면 다음 날 보고서의 부하 절을 보면 된다 - 낮 실행도
따로 기록되어 그 줄에 나온다.

**이제는 감으로 답하지 않아도 된다.** 며칠 쌓인 뒤 `smvwp_cli.py load` 를
돌리면 시간대마다 다른 작업이 실제로 얼마나 쓰고 있는지 나온다. 아래
[서버 부하 이력](#서버-부하-이력---smvwp_clipy-load) 참고.

---

## 지난 밤이 서버에 얼마나 부담이었나

일간 보고서의 `[스캔 중 리소스 변화]` 절이 답한다.

```
[스캔 중 리소스 변화]
------------------------------------------------------------------------
2026-08-24 22:00:00 · 자동(cron) · 7시간 10분 · completed
표본 6개 · 동시 실행 볼륨 1개 기준

같은 기간의 다른 실행 (이것도 부하를 만들었습니다):
  2026-08-25 14:00:00 · 화면에서 수동 실행 · 5분 · completed

지표                 스캔 전  스캔 중 평균        최고      증가폭
load(1분)               0.31          5.90        5.90       +5.59
I/O대기                 0.4%          9.0%        9.0%       +8.6%
CPU                     2.0%          6.0%        6.0%       +4.0%
```

읽는 법:

- **첫 줄이 어느 실행의 숫자인지** 말한다. 낮에 손으로 돌린 것과 밤 것을
  섞어 읽지 않게 하려는 것이다. 여러 개가 있으면 **가장 오래 돈 것**을
  고른다 - 아침에 알고 싶은 것은 밤이지 낮에 눌러 본 5분짜리가 아니다.
- **`스캔 전` 이 기준값이다.** 스캔을 시작하기 직전에 잰 값이라, 늘어난 만큼이
  이 도구가 만든 부담이다.
- **CPU 가 낮아도 안심하면 안 된다.** 실제 부담은 대개 파일서버 쪽 I/O 인데
  그것은 이 서버에서 관측되지 않는다. `I/O대기` 와 `load` 가 올랐다면 부하가
  없었던 것이 아니다.
- 아래 `시간 흐름` 표는 밤새 부하가 어떻게 움직였는지 보여 준다. 한 구간만
  튀었는지 내내 높았는지가 여기서 갈린다.

주간 보고서에는 밤끼리 비교한 표가 따로 있다.

---

### 그때 서버에서 무엇이 돌았나

부하 숫자만으로는 `load 8` 이 우리 스캔이 만든 것인지 마침 다른 사람의 작업
때문인지 가릴 수 없다. 그래서 같은 시각에 **서버에서 무엇이 돌고 있었는지**도
함께 남기고, 보고서에 절을 하나 더 붙였다.

```
[그때 서버에서 무엇이 돌았나]
------------------------------------------------------------------------
우리 스캔을 뺀 나머지가 쓴 CPU: 평균 11.2%, 최고 24.0% · I/O 를 기다린 남의 작업 평균 1.0개

사용자        작업                    CPU최고   메모리최고
alice         virtuoso                 180.0%       9.0 GB
bob           calibre                   95.0%      21.0 GB

/mnt/caef1201 - 22,800,000건, 왕복 0.16ms, 대기 0.31ms
  대기가 왕복보다 크면 병목은 파일서버가 아니라 이쪽 RPC 슬롯입니다.
```

- **`우리 스캔을 뺀 나머지`** 가 핵심입니다. 이 값이 낮으면 그날 밤 서버는
  거의 우리 차지였다는 뜻이고, 높으면 다른 작업과 겹친 밤이라 부하 숫자를
  그대로 "스캔이 만든 것"으로 읽으면 안 됩니다.
- **왕복(rtt)과 대기(queue)를 나눠 봅니다.** 왕복이 길면 파일서버가 밀리는
  것이고, 대기가 길면 요청이 **보내지지도 못하고** 우리 쪽에서 줄을 선 것입니다.
  뒤쪽인데 병렬을 줄이면 정확히 반대 처방이 됩니다.

---

## 용량 추세

df 표본을 90일치 쌓고 있다. 홈 표의 `추세` 칸이 최근 30일 모양을 보여 주고,
숫자와 축이 필요하면 `추세 보기` 버튼으로 창을 연다.

읽을 때 알아야 할 것 둘.

- **세로축은 0~100% 로 고정이다.** 데이터에 맞춰 늘이면 78%에서 80%로 간 것이
  화면을 꽉 채워 큰일이 난 것처럼 보인다. 점선은 주의(90%)와 경고(95%)다.
- **선이 끊긴 곳은 그때 수집기가 돌지 않은 것이다.** 이어 그으면 없는 값을
  있는 것처럼 보여주게 되므로 끊어 둔다. 몇 칸이 비었는지는 툴팁에 나온다.

칸의 값은 그 구간의 **마지막** 값이다. 최고값을 쓰면 잠깐 찼다가 정리된 순간이
계속 남아 "줄지 않는 것처럼" 보인다.

---

## 서버 부하 이력 - `smvwp_cli.py load`

보고서는 그날 밤 하나만 말합니다. **평소 이 서버가 어떤 모습인가**는 따로
쌓아야 알 수 있고, 그래야 "낮에 상세 스캔을 돌려도 되나" 같은 판단이 감이
아니게 됩니다.

수집기가 15분마다 돌면서 한 벌씩 남깁니다. 따로 등록할 것은 없습니다 -
`collect` 가 이미 cron 에 있으면 그날부터 쌓입니다.

화면으로 보려면 대시보드의 `서버 부하` 버튼을 누른다. 기간(7/30/90일)과
작업 정렬(CPU/메모리)을 바꿔 가며 볼 수 있고, 명령줄은 작업 이름에 마우스를
올리면 나온다.

터미널에서는 이렇게 본다.

```bash
smvwp_cli.py load
```

```bash
smvwp_cli.py load --days 30 --by mem
```

무엇이 나오는가:

1. **스캔 중 vs 평소** - 같은 서버의 두 상태를 나란히 놓습니다.
2. **시간대별** - 다른 작업이 쓰는 CPU 를 시간대마다. 하루 평균은 낮 두 시와
   새벽 세 시를 섞어 버려서 아무 말도 안 해 줍니다.
3. **다른 작업이 가장 적었던 시간대** - 우리 스캔이 돌던 시간대는 뺍니다.
   새벽이 한가한 것은 이미 우리가 그 자리를 쓰고 있어서라, 그 칸을 "여기에 더
   얹으세요"로 내밀면 근거가 못 됩니다.
4. **서버를 쓴 작업** - 사용자와 작업 이름으로 묶어서. `--by mem` 으로 메모리
   기준 정렬. CPU 는 거의 안 쓰면서 메모리만 20GB 물고 있는 작업은 CPU 정렬에
   영영 안 잡힙니다.
5. **NFS 마운트** - 어디에 얼마나 실었고 얼마나 느렸는지.

주의해서 읽을 것:

- **표본이 넷 미만인 시간대에는 판정을 내리지 않습니다.** 두세 벌로 "여유
  있음"이라고 하면 측정이 아니라 추측입니다. 며칠은 쌓여야 합니다.
- **작업 목록의 `CPU평균` 은 상위 목록에 들었던 표본들만의 평균입니다.**
  한가할 때는 목록에 못 들어 빠지므로 하루 평균보다 높게 나옵니다. "이 작업이
  평소 쓰는 양"으로 읽으면 안 됩니다.

### 무엇이 기록되는가

| 무엇 | 주기 | 보관 |
|---|---|---|
| 서버 전체 수치 (CPU, 메모리, load, 대기) | 15분 (수집기) | 90일 |
| 같은 것, 스캔이 도는 동안 | 30초 | 30일 |
| 그때 돌던 작업 상위 10개 | 15분 / 스캔 중 2분 | 위와 같음 |
| NFS 마운트별 ops·지연 | 위와 같음 | 위와 같음 |

프로세스 목록은 부하 숫자보다 뜸하게 뜹니다. `/proc` 을 수백 번 읽는 일이라
30초마다 밤새 하면 **재는 쪽이 부담**이 되고, 남는 행도 하룻밤에 만 줄이
넘습니다.

### 다른 사용자의 명령줄

작업 목록에는 **명령줄이 함께 남습니다.** 이름만으로는 `python` 이 수십 개
나올 뿐이라 어떤 작업인지 알아보려면 필요하지만, 그 명령줄에는 다른 사용자의
작업 내용(프로젝트 이름, 파일 경로 등)이 그대로 들어갑니다.

남길지는 운영하는 쪽이 정할 일이라 스위치를 뒀습니다. 끄면 프로세스 이름과
사용자만 남습니다.

```
record_process_cmdline: false
```

---

## 누가 이 도구를 쓰는가 - `smvwp_cli.py usage`

몇 명이 쓸지, 무엇을 보러 들어오는지, 스캔을 직접 돌리는 사람이 있는지를
감으로 정하면 아무도 안 쓰는 기능에 공을 들이게 됩니다. 물어봐서 알 수 있는
것도 아닙니다 - 사람은 자기가 무엇을 얼마나 쓰는지 잘 기억하지 못하고,
특히 **안 쓰는 것**은 화제에 오르지도 않습니다.

```bash
smvwp_cli.py usage
```

```bash
smvwp_cli.py usage --days 90 --recent 20
```

- **`쓴 날` 이 `횟수` 보다 중요합니다.** 하루에 창을 열 번 연 사람과 열흘 동안
  매일 한 번씩 연 사람은 횟수가 같아도 전혀 다른 이야기입니다 - 뒤쪽만이 이
  도구가 일과에 들어갔다는 뜻입니다.
- 기능마다 **쓴 사람 수**를 함께 봅니다. 한 사람이 백 번 쓴 기능과 열 사람이
  열 번씩 쓴 기능은 같은 숫자라도 뜻이 다릅니다.

남기는 것은 **굵직한 행동뿐**입니다 - 창을 열었다, 스캔을 손으로 돌렸다,
보고서를 봤다, 검색을 썼다. 탭을 옮기거나 표를 정렬한 것까지 남기면 행 수만
불고 정작 알고 싶은 것이 잡음에 묻힙니다.

**내용은 남기지 않습니다.** 검색을 했다는 사실은 남기지만 무엇을 찾았는지는
남기지 않습니다. cron 이 돈 스캔도 남기지 않습니다 - 그것은 실행 이력에 이미
있고, 여기서 알고 싶은 것은 사람이 직접 돌리는 일이 있는가입니다.

---

## 멈추는 방법 - `kill` 을 쓰지 않는다

```bash
smvwp_cli.py scan --stop
```

| | `--stop` | `kill` |
|---|---|---|
| 언제 멈추나 | 다음 책갈피에서 (최대 15분) | 즉시 |
| 재던 디렉터리 | 끝까지 재고 **저장한 뒤** 멈춤 | **통째로 버려짐** |
| 실행 기록 | `stopped` 로 마감 | `running` 인 채로 남음 |
| 다음 밤 | 이어받음 | 이어받음 (버린 것 하나만 다시) |

책갈피 하나가 디렉터리 하나이고 예산이 15분이다. 그래서 `--stop` 은 즉시
멈추지 않는다 - 지금 재고 있는 디렉터리를 마저 끝내고 멈춘다. 그 15분이
**버려지지 않게 하는** 대가다.

`kill` 은 그 작업이 통째로 사라진다. 300GB 짜리를 14분째 재고 있었다면 그
14분이 날아가고 다음 밤에 처음부터 다시 한다.

---

## 안 도는 것 같을 때

| 증상 | 볼 곳 |
|---|---|
| 아무것도 안 쌓임 | GUI 상세 스캔 탭의 **cron 표시 줄** |
| 밤 스캔만 안 돎 | 같은 줄 (야간 항목이 빠졌는지) |
| 진행이 더딤 | `smvwp_cli.py scan --status` |
| 환경 자체가 의심스러움 | `./run.csh --diagnose` |
| 성능을 재고 싶음 | `smvwp_cli.py probe --path <경로>` (`PROBE_CODES.md`) |

---

## 속도를 올리고 싶을 때

실측으로 확인된 것 두 가지다.

**한 계정 안에서는 동시 4가 천장이다.** `checkpoint_workers` 를 8, 16, 32 로
올려도 안 빨라진다(1.65배에서 평평). CPU 도 슬롯도 남는데 마운트 하나당
처리량이 정해져 있기 때문이다. **4 그대로 두는 것이 맞다.**

**서로 다른 export 는 동시에 돌려도 각자 속도를 지킨다.** 세 번 재서 0.90 /
1.02 / 1.04 였다. 계정들이 몇 개 export 에 걸쳐 있는지 세어 보고,

```bash
df -P /계정경로1 /계정경로2 /계정경로3 | awk 'NR>1{print $1}' | sort -u | wc -l
```

그 수만큼 계정관리 설정의 `야간 동시 스캔` 을 올리면 그만큼 밤이 짧아진다.

---

## 이 도구가 하지 않는 것

- **아무것도 지우지 않는다.** 정리 후보 보고서는 목록일 뿐이고, 지우는 것은
  사람이 한다.
- **모니터링 대상에 쓰지 않는다.** 읽기만 한다.
- 계정 경로 밖은 건드리지 않는다.
